# 지역소개 강화 + 표시 버그 2건 (F1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 위키박스를 네이버 실측 키워드 근거로 풍부하게 만들고, `##` 마크다운 노출과 `PLACE N` 미채번 버그를 고친다.

**Architecture:** ① `_generate_region_desc`에 `naver_analysis` 근거 주입(+생성 라우트에서 자동 SEO 확보, 실패 무해), ② `html_to_blocks`의 일반 h2/h3를 마크다운 텍스트 대신 heading 블록으로, ③ `PLACE N`을 코드 후처리로 순서 채번(순수함수). 환각 방지 원칙(확실한 것만·하한 없음)과 "형식은 코드" 불변식 유지.

**Tech Stack:** Python 3, Flask, pytest(monkeypatch). 기준 커밋 `635fd30` — 라인 어긋나면 함수명/문자열로 찾는다.

---

## File Structure

- `core.py` — `_number_places` 신규+적용(Task 1), `_generate_region_desc` 근거 주입+`generate_drafts` 전달(Task 3)
- `posters.py` — `html_to_blocks` h2/h3→heading 블록(Task 2)
- `app.py` — `api_generate`/`api_generate_all` 자동 SEO 확보(Task 3)
- `tests/test_quality.py` — 3개 테스트 (44 → 47 passed)

---

### Task 1: `_number_places` — PLACE 순서 채번 (순수)

**Files:**
- Modify: `core.py` — `TravelBlogGenerator`에 staticmethod 추가(`_dedupe_separators` 근처가 무난) + `generate_drafts` 적용 1줄
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test** (파일 끝에 추가)

```python
def test_number_places():
    f = TravelBlogGenerator._number_places
    html = ('<p style="letter-spacing:3px">PLACE N</p><p>본문</p>'
            '<p style="letter-spacing:3px">PLACE N</p>')
    out = f(html)
    assert "PLACE 1" in out and "PLACE 2" in out and "PLACE N" not in out
    # 이미 채번된 것은 무변경
    done = '<p>PLACE 1</p><p>PLACE 2</p>'
    assert f(done) == done
    # PLACE 없음 → 무변경, 빈 입력 안전
    assert f("<p>그냥 본문</p>") == "<p>그냥 본문</p>"
    assert f("") == ""
```

- [ ] **Step 2: Run to confirm FAIL**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_number_places -q`
Expected: FAIL (AttributeError)

- [ ] **Step 3: Implement**

`TravelBlogGenerator`에 추가:

```python
    @staticmethod
    def _number_places(content):
        """AI가 채번하지 않고 남긴 'PLACE N' 리터럴을 등장 순서대로 1,2,3…으로 치환.
        (형식은 코드가 조립 — AI 채번 의존 제거) 이미 숫자면 무변경. 순수함수."""
        if not content or "PLACE N" not in content:
            return content or ""
        counter = {"n": 0}
        def _sub(m):
            counter["n"] += 1
            return f"PLACE {counter['n']}"
        return re.sub(r"PLACE N\b", _sub, content)
```

그리고 `generate_drafts`의 post 완성 블록에서 `post["style"] = s["name"]` 바로 다음 줄에 추가:

```python
                if post.get("content"):
                    post["content"] = self._number_places(post["content"])
```

- [ ] **Step 4: Run full suite**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 45 passed

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "fix(gen): PLACE N 미채번 — 코드 후처리로 순서 채번(_number_places)"
```

---

### Task 2: `html_to_blocks` 일반 h2/h3 → heading 블록 (`##` 노출 수정)

**Files:**
- Modify: `posters.py` — `html_to_blocks` 내 3-3) 블록 뒤(~163행)와 텍스트 정리부(~353-354행)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_html_to_blocks_no_markdown_heading():
    from posters import html_to_blocks
    html = ('<h3 style="text-align:center">📍 오사카시 여행 전 꼭 알아야 할 핵심 정보</h3>'
            '<p>오사카시는 일본의 간사이 지방에 위치한 대도시입니다.</p>'
            '<h2>여행 꿀팁</h2>')
    blocks = html_to_blocks(html)
    texts = [b.get("content", "") for b in blocks if b.get("type") == "text"]
    # 마크다운 리터럴이 텍스트 블록에 남지 않는다 (## 노출 버그 회귀 방지)
    assert not any(t.strip().startswith("#") for t in texts)
    assert not any("##" in t for t in texts)
    heads = [b for b in blocks if b.get("type") == "heading"]
    assert any("핵심 정보" in h.get("content", "") for h in heads)
    assert any("여행 꿀팁" in h.get("content", "") for h in heads)
```

- [ ] **Step 2: Run to confirm FAIL**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_html_to_blocks_no_markdown_heading -q`
Expected: FAIL (현재 h3가 `## …` 텍스트 블록으로 나옴)

- [ ] **Step 3: Implement**

(a) `posters.py`의 `html_to_blocks` 안, 3-3) data-place h2 블록(끝 ~163행) 바로 뒤에 추가:

```python
    # 3-4) 일반 h2/h3 → heading 블록 (마크다운 '#' 리터럴 노출 버그 수정)
    for m in _re.finditer(r'<h([23])[^>]*>(.*?)</h\1>', html_content,
                          _re.DOTALL | _re.IGNORECASE):
        if not any(s <= m.start() < e for s, e, _ in special_ranges):
            title_text = _re.sub(r'<[^>]+>', '', m.group(2)).strip()
            if title_text:
                special_ranges.append((m.start(), m.end(),
                    {"type": "heading", "content": title_text,
                     "place": "", "style": ""}))
```

(b) 텍스트 정리부의 이 두 줄(~353-354행):

```python
    t = re.sub(r'<h2[^>]*>(.*?)</h2>', r'\n\n# \1\n', t)
    t = re.sub(r'<h3[^>]*>(.*?)</h3>', r'\n## \1\n', t)
```

을 아래로 교체 (h2/h3는 전부 heading 블록으로 처리됐으므로 텍스트에서 제거 — 이중 등장 방지):

```python
    # h2/h3 제거 (3-4에서 heading 블록으로 처리됨 — 마크다운 변환 금지)
    t = re.sub(r'<h[23][^>]*>.*?</h[23]>', '', t, flags=re.DOTALL)
```

주의: 25행 `_html_to_text`의 동일 정규식은 **건드리지 않는다** (스마트에디터 붙여넣기용 의도된 변환).

- [ ] **Step 4: Run full suite**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 46 passed (기존 html_to_blocks 소비 테스트 포함 회귀 없음)

- [ ] **Step 5: Commit**

```bash
git add posters.py tests/test_quality.py
git commit -m "fix(blocks): 일반 h2/h3를 heading 블록으로 — '##' 마크다운 리터럴 노출 수정"
```

---

### Task 3: 위키박스 실측 근거 주입 + 생성 시 자동 SEO 확보

**Files:**
- Modify: `core.py:2090-2120` (`_generate_region_desc`), `core.py:1902` (`generate_drafts` 호출)
- Modify: `app.py` — `api_generate`/`api_generate_all`의 task() 시작부
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_region_desc_uses_naver_evidence(monkeypatch):
    import types, json as _json
    g = TravelBlogGenerator(Config())
    captured = {}
    fake = types.SimpleNamespace(choices=[types.SimpleNamespace(
        message=types.SimpleNamespace(content=_json.dumps(
            {"intro": "소개", "specialties": [], "foods": [], "spots": []})))])
    def fake_create(**k):
        captured["messages"] = k["messages"]; return fake
    monkeypatch.setattr(g.client.chat.completions, "create", fake_create)
    na = {"common_keywords": ["료칸", "카메노이호텔"], "top_titles": ["기타큐슈 여행기 총정리"]}
    g._generate_region_desc("기타큐슈", na)
    user = captured["messages"][1]["content"]
    assert "료칸" in user and "실측 참고" in user and "무시" in user
    # 근거 미전달 → 기존 프롬프트 그대로 (회귀)
    g._generate_region_desc("기타큐슈")
    user2 = captured["messages"][1]["content"]
    assert "실측 참고" not in user2
```

- [ ] **Step 2: Run to confirm FAIL**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_region_desc_uses_naver_evidence -q`
Expected: FAIL (`_generate_region_desc`가 두 번째 인자를 안 받음 → TypeError)

- [ ] **Step 3: Implement core.py**

(a) `_generate_region_desc` 시그니처와 프롬프트를 교체. 현재:

```python
    def _generate_region_desc(self, region_hint):
```

→

```python
    def _generate_region_desc(self, region_hint, naver_analysis=None):
```

메서드 안 `try:` 바로 위(exclude 체크 뒤)에 근거 조각 구성 추가:

```python
        # 실측 근거(E의 네이버 상위 블로그 분석) — 있으면 재료로 제공, 환각 방지 지시 유지
        evidence = ""
        na = naver_analysis or {}
        kws = [k for k in (na.get("common_keywords") or []) if k][:10]
        top_titles = [t for t in (na.get("top_titles") or []) if t][:5]
        if kws or top_titles:
            evidence = (
                "\n[실측 참고 — 이 지역 검색 상위 블로그의 빈출 키워드/제목입니다. "
                "이 중 당신이 확실히 아는 것만 반영하고, 모르는 것은 무시하세요.]\n"
                + (f"키워드: {', '.join(kws)}\n" if kws else "")
                + (f"상위 글 제목: {' / '.join(top_titles)}\n" if top_titles else "")
            )
```

user 메시지 content를 아래로 교체 (evidence 삽입 + 분량 상향: 2~4줄→3~5줄, 0~4개→0~6개):

```python
                    {"role": "user", "content":
                        f"'{region_hint}' 여행 전 꼭 알아야 할 핵심 정보를 아래 JSON 형식으로만 응답하세요.\n"
                        f"모든 값은 한국어. specialties/foods/spots는 각 0~6개 배열 — "
                        f"'{region_hint}' 안에 실제로 있고 전국적으로 유명한 것만 넣으세요. "
                        f"확실하지 않으면 빈 배열로 두세요(개수 채우기 절대 금지).\n"
                        f"{evidence}"
                        '{"intro":"3~5줄 지역 소개(확실한 사실만)","specialties":[],'
                        '"foods":[],"spots":[]}'}
```

파싱부 상한도 `[:5]` 3곳 → `[:6]`, `max_tokens=500` → `max_tokens=700`.

(b) `generate_drafts`의 호출(core.py:1902):

```python
        region_desc = self._generate_region_desc(region)
```

→

```python
        region_desc = self._generate_region_desc(region, naver_analysis)
```

(`naver_analysis`는 `generate_drafts`의 기존 인자.)

- [ ] **Step 4: Implement app.py — 자동 SEO 확보**

`api_generate`의 `task()` 안 `try:` 바로 다음, `_apply_settings_to_generator()` 앞에 추가. `api_generate_all`의 `task()`에도 동일하게 추가 (두 곳):

```python
            # 위키박스 실측 근거: SEO 분석이 없으면 자동 1회 확보(실패해도 생성 계속)
            if not state.get("naver_analysis"):
                try:
                    _plan = state.get("plan") or {}
                    _kw = (_plan.get("region") or "").strip()
                    if _kw:
                        state["progress"]["message"] = "지역 상위 블로그 분석 중..."
                        state["naver_analysis"] = NaverBlogAnalyzer(state["cfg"]).analyze(_kw + " 여행")
                except Exception as _e:
                    logger.warning(f"자동 SEO 분석 실패(무시): {_e}")
```

(`NaverBlogAnalyzer`·`logger`는 app.py:16에서 이미 import됨.)

- [ ] **Step 5: Run full suite + import check**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q` → 47 passed
(기존 `test_region_desc_no_wiki_label`은 naver_analysis 미전달 경로 — 그대로 통과해야 함)
Run: `venv/Scripts/python.exe -c "import core, app; print('ok')"` → ok

- [ ] **Step 6: Commit**

```bash
git add core.py app.py tests/test_quality.py
git commit -m "feat(wiki): 지역 위키박스에 네이버 실측 키워드 근거 주입 + 생성 시 자동 SEO 확보"
```

---

## 최종 검증

- [ ] `venv/Scripts/python.exe -m pytest tests/test_quality.py -q` → 47 passed
- [ ] 서버 재시작 후 실생성 1회: 위키박스 항목 수·intro 길이 증가 확인, 미리보기에 `##` 부재, `PLACE 1/2` 채번 확인

## Self-Review 결과

- **Spec 커버리지:** §2a(근거 주입·상향·자동 확보)=Task 3, §2b(heading 블록)=Task 2, §2c(채번)=Task 1, §4 테스트 3개 전부 대응. `_html_to_text` 비수정 주의 명시.
- **Placeholder:** 없음.
- **타입 일관성:** `_number_places(content)->str`, `_generate_region_desc(region_hint, naver_analysis=None)`, heading 블록 스키마 `{type,content,place,style}`(기존 3-3과 동일) 일관.
