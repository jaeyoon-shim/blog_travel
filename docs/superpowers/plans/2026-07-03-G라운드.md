# 테스트 피드백 G라운드 (G1~G4·G6) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 초안 영구 보존(G1), 발행 후 다음 일차 흐름(G2), 인라인 블록 편집(G3), 지역소개·지시 강화(G4), 제목 최적화(G6).

**Architecture:** core는 순수 확장(위키박스 스키마·지시 섹션·제목 최적화), app은 drafts 파일 저장/복원 + blocks의 photos 폴백, UI는 renderS3/S4/S5 3곳. 모든 신규 단계는 실패 시 기존 결과 유지.

**Tech Stack:** 기준 커밋 `b5af7d2`. `venv/Scripts/python.exe` 고정. UI 수정 후 `node vm.Script` 파싱 필수.

---

## File Structure

- `core.py` — T1(위키박스 tips/access), T2(지시 격상), T3(optimize_title)
- `app.py` — T4(drafts 저장/복원·자동훅·blocks 폴백·status)
- `static/index.html` — T5(인라인 편집), T6(STEP4 저장버튼 + STEP6 일차탭·다음일차)
- `tests/test_quality.py` — 4개 (50 → 54)

---

### Task 1 (G4a): 위키박스 tips/access 확장

**Files:** Modify `core.py` `_generate_region_desc`; Test `tests/test_quality.py`

- [ ] **Step 1: failing test**

```python
def test_region_desc_tips_access(monkeypatch):
    import types, json as _json
    g = TravelBlogGenerator(Config())
    fake = types.SimpleNamespace(choices=[types.SimpleNamespace(
        message=types.SimpleNamespace(content=_json.dumps({
            "intro": "소개", "specialties": [], "foods": ["라멘"], "spots": [],
            "tips": ["겨울엔 방한 필수"], "access": ["공항에서 지하철 30분"]})))])
    monkeypatch.setattr(g.client.chat.completions, "create", lambda **k: fake)
    box = g._generate_region_desc("기타큐슈")
    assert "💡" in box and "방한" in box
    assert "🚌" in box and "지하철" in box
    # 빈 배열이면 행 생략
    fake2 = types.SimpleNamespace(choices=[types.SimpleNamespace(
        message=types.SimpleNamespace(content=_json.dumps({
            "intro": "소개", "specialties": [], "foods": [], "spots": [],
            "tips": [], "access": []})))])
    monkeypatch.setattr(g.client.chat.completions, "create", lambda **k: fake2)
    box2 = g._generate_region_desc("기타큐슈")
    assert "💡" not in box2 and "🚌" not in box2
```

- [ ] **Step 2: confirm FAIL** — `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_region_desc_tips_access -q` (💡/🚌 부재로 assert 실패)

- [ ] **Step 3: implement** — `_generate_region_desc`에서:

(a) user 프롬프트의 JSON 형식 줄을 교체:
```python
                        '{"intro":"4~6줄 지역 소개(확실한 사실만)","specialties":[],'
                        '"foods":[],"spots":[],"tips":[],"access":[]}'}
```
그리고 그 위 설명 문장 `f"모든 값은 한국어. specialties/foods/spots는 각 0~6개 배열 — "` 을
`f"모든 값은 한국어. specialties/foods/spots는 각 0~6개, tips(여행 실용 팁)는 0~3개, access(대표 접근 교통)는 0~2개 배열 — "` 로 교체.

(b) `max_tokens=700` → `max_tokens=900`.

(c) 파싱부 `spots = ...` 다음에 추가:
```python
            tips = [s for s in (data.get("tips") or []) if s][:3]
            access = [s for s in (data.get("access") or []) if s][:2]
```
`if not intro and not (specialties or foods or spots):` → `if not intro and not (specialties or foods or spots or tips or access):`

(d) rows 조립에 추가(spots 다음):
```python
            if access:
                rows.append(("🚌 가는 법", " · ".join(access)))
            if tips:
                rows.append(("💡 여행 팁", " · ".join(tips)))
```
로그 라인도 `f"...관광지{len(spots)}/팁{len(tips)}/교통{len(access)})"` 로 갱신.

- [ ] **Step 4: full suite** → 51 passed
- [ ] **Step 5: commit** — `feat(wiki): 위키박스에 여행 팁·가는 법 행 추가(0~3/0~2, 하한 없음)`

---

### Task 2 (G4b): 장소별 지시 격상 (`place_directives`)

**Files:** Modify `core.py` — TripPlanner 그룹 조립(~1640-1674) · `generate_drafts` · `_build_prompt`; Test

- [ ] **Step 1: failing test**

```python
def test_build_prompt_directives_section():
    g = TravelBlogGenerator(Config())
    photos = [{"location_name": "고쿠라 성", "file_name": "a.jpg",
               "gps": {"lat": 33.88, "lon": 130.87}}]
    style = {"name": "감성", "desc": "감성적"}
    p = g._build_prompt(photos, "요약", "코스", "기타큐슈", "", "그룹", "제목", style, "", "",
                        place_directives={"고쿠라 성": "야경 사진 위주로 강조해줘"})
    assert "장소별 필수 지시" in p and "야경 사진 위주" in p and "우선" in p
    p2 = g._build_prompt(photos, "요약", "코스", "기타큐슈", "", "그룹", "제목", style, "", "")
    assert "장소별 필수 지시" not in p2
```

- [ ] **Step 2: confirm FAIL** — TypeError(unexpected keyword)

- [ ] **Step 3: implement**

(a) TripPlanner 그룹 조립부(현재 `bits.append("지시: " + s["ai_instruction"])` 부근) — 지시를 memo에서 분리:

현재:
```python
                if s.get("ai_instruction"):
                    bits.append("지시: " + s["ai_instruction"])
```
를 제거하고, 루프 밖 `memos = {}` 선언 옆에 `directives = {}` 추가한 뒤 같은 자리에:
```python
                if s.get("ai_instruction"):
                    directives[name] = s["ai_instruction"]
```
그룹 dict(`"place_memos": memos,` 다음)에 `"place_directives": directives,` 추가.
(주의: `memos`/`directives`는 day 루프 안에서 stop 루프 위에 선언되는 기존 구조를 따른다 — `memos = {}` 선언 위치를 찾아 나란히.)

(b) `generate_drafts`: `place_memos = group.get("place_memos", {})` 다음에
```python
        place_directives = group.get("place_directives", {})
```
`self._build_prompt(...)` 호출에 `place_directives=place_directives,` 인자 추가.

(c) `_build_prompt` 시그니처: `place_memos=None` 뒤에 `place_directives=None` 추가. `memo_ctx` 블록 다음에:
```python
        # 장소별 필수 지시 (plan 검수에서 입력) — 일반 메모보다 격상, 최우선 준수
        directive_ctx = ""
        if place_directives:
            d_lines = [f"  - {loc}: {d}" for loc, d in place_directives.items() if str(d).strip()]
            if d_lines:
                directive_ctx = (
                    "\n[장소별 필수 지시 — 아래 지시는 반드시 그대로 따르세요. "
                    "다른 규칙과 충돌하면 이 지시가 우선합니다]\n"
                    + "\n".join(d_lines) + "\n"
                )
```
그리고 프롬프트 본문에서 `{memo_ctx}`가 삽입되는 지점을 찾아 `{memo_ctx}{directive_ctx}` 로 확장(현재 f-string에 `{naver_ctx}{style_ctx}{struct_ctx}{memo_ctx}{custom_style_ctx}` 형태 — `memo_ctx` 바로 뒤에 넣는다).

- [ ] **Step 4: full suite** → 52 passed
- [ ] **Step 5: commit** — `feat(gen): 장소별 지시(ai_instruction)를 필수 지시 섹션으로 격상(place_directives)`

---

### Task 3 (G6): 제목 최적화 (`optimize_title`)

**Files:** Modify `core.py` — `TravelBlogGenerator`에 메서드 추가 + `generate_drafts` 배선; Test

- [ ] **Step 1: failing test**

```python
def test_optimize_title_pick_and_fallback(monkeypatch):
    import types, json as _json
    g = TravelBlogGenerator(Config())
    na = {"top_titles": ["기타큐슈 여행 코스 총정리", "기타큐슈 맛집 BEST 5"],
          "common_keywords": ["라멘", "맛집"], "avg_title_length": 20}
    post = {"title": "고요한 신성함의 기록", "content": "<p>" + "본문" * 200 + "</p>"}
    captured = {}
    def fake_create(**k):
        captured["prompt"] = k["messages"][0]["content"]
        return types.SimpleNamespace(choices=[types.SimpleNamespace(
            message=types.SimpleNamespace(content=_json.dumps({"titles": [
                "너무길다" * 20,                      # 45자 초과 → 탈락
                "감성 여행기",                        # 지역명 없음 → 탈락
                "기타큐슈 라멘 여행 코스 총정리"]})))])  # 통과 → 채택
    monkeypatch.setattr(g.client.chat.completions, "create", fake_create)
    t = g.optimize_title(post, na, "기타큐슈")
    assert t == "기타큐슈 라멘 여행 코스 총정리"
    assert "기타큐슈 여행 코스 총정리" in captured["prompt"]   # 실측 상위 제목이 근거로 포함
    # 전부 탈락 → None (기존 제목 유지용)
    def all_bad(**k):
        return types.SimpleNamespace(choices=[types.SimpleNamespace(
            message=types.SimpleNamespace(content=_json.dumps({"titles": ["지역명없는제목"]})))])
    monkeypatch.setattr(g.client.chat.completions, "create", all_bad)
    assert g.optimize_title(post, na, "기타큐슈") is None
    # 분석 없음 → None (호출 안 함)
    assert g.optimize_title(post, None, "기타큐슈") is None
```

- [ ] **Step 2: confirm FAIL** — AttributeError

- [ ] **Step 3: implement** — `TravelBlogGenerator`에 추가(`seo_check` 뒤):

```python
    def optimize_title(self, post, naver_analysis, region):
        """네이버 상위 실측 제목 패턴 기반 제목 최적화. 노출 관련은 코드가 강제:
        후보 3개 생성 → (지역명 포함 AND 15~45자) 첫 통과 후보 채택.
        분석 없음/전부 탈락/LLM 실패 → None (기존 제목 유지, seo_check가 경고)."""
        na = naver_analysis or {}
        top_titles = [t for t in (na.get("top_titles") or []) if t][:10]
        region = (region or "").strip()
        if not top_titles or not region:
            return None
        kws = [k for k in (na.get("common_keywords") or []) if k][:8]
        body = re.sub(r'<[^>]+>', ' ', (post or {}).get("content", "") or "")[:800]
        prompt = (
            f"'{region}' 여행 블로그 글의 검색 노출용 제목을 만들어라.\n"
            "[실측 — 이 키워드로 네이버 상위에 노출된 실제 제목들]\n"
            + "\n".join(f"- {t}" for t in top_titles)
            + (f"\n[연관 키워드] {', '.join(kws)}" if kws else "")
            + f"\n[글 내용 요약]\n{body}\n\n"
            "[규칙] 상위 제목들의 구조·키워드 배치·길이 패턴을 모방하되 그대로 베끼지 말 것. "
            f"'{region}'을(를) 반드시 포함. 낚시·과장·이모지 금지. 글 내용에 실제로 있는 것만. "
            '서로 다른 제목 후보 3개를 JSON으로만: {"titles":["...","...","..."]}'
        )
        try:
            r = self.client.chat.completions.create(
                model=self.model,
                messages=[{"role": "user", "content": prompt}],
                response_format={"type": "json_object"},
                temperature=0.5, max_tokens=300,
            )
            cands = (json.loads(r.choices[0].message.content).get("titles") or [])
        except Exception as e:
            logger.warning(f"⚠️ 제목 최적화 실패(기존 제목 유지): {e}")
            return None
        for c in cands:
            c = str(c).strip()
            if region in c and 15 <= len(c) <= 45:
                return c
        return None
```

`generate_drafts`의 post 완성 블록에서 `post["seo_warnings"] = ...` **앞**에 삽입(제목 확정 후 경고 계산):

```python
                # 제목 최적화 (G6) — 실측 상위 제목 패턴 기반, 노출 관련은 코드가 강제
                best_title = self.optimize_title(post, naver_analysis, region)
                if best_title:
                    post["original_title"] = post.get("title", "")
                    post["title"] = best_title
```

- [ ] **Step 4: full suite** → 53 passed
- [ ] **Step 5: commit** — `feat(seo): 제목 최적화(optimize_title) — 상위 실측 제목 패턴+코드 검증 채택`

---

### Task 4 (G1): 초안 파일 저장/복원 (app.py)

**Files:** Modify `app.py`

- [ ] **Step 1: failing test**

```python
def test_drafts_save_load_roundtrip(tmp_path, monkeypatch):
    import app as app_mod
    monkeypatch.setitem(app_mod.state, "plan", {"trip_title": "테스트 여행"})
    monkeypatch.setattr(app_mod, "_plan_dir", lambda: str(tmp_path))
    app_mod.state["group_states"] = {0: {"drafts": [{"title": "t", "content": "<p>c</p>", "tags": ["a"]}]}}
    app_mod._save_drafts(0)
    app_mod.state["group_states"] = {}
    loaded = app_mod._load_saved_drafts(0)
    assert loaded and loaded[0]["title"] == "t"
    assert app_mod._load_saved_drafts(99) is None   # 없는 그룹 → None
```

- [ ] **Step 2: confirm FAIL** — AttributeError(`_save_drafts` 없음)

- [ ] **Step 3: implement** — `_save_plan` 아래에 헬퍼 2개:

```python
def _save_drafts(gi):
    """생성/재작성된 초안을 plan 폴더에 영구 저장 (서버 재시작 대비). 실패는 로그만."""
    try:
        drafts = state["group_states"].get(gi, {}).get("drafts")
        if not drafts:
            return
        d = os.path.join(_plan_dir(), "drafts")
        os.makedirs(d, exist_ok=True)
        with open(os.path.join(d, f"group_{gi}.json"), "w", encoding="utf-8") as f:
            json.dump(drafts, f, ensure_ascii=False)
    except Exception as e:
        logger.warning(f"초안 저장 실패(무시): {e}")


def _load_saved_drafts(gi):
    """저장된 초안 파일 로드. 없으면 None."""
    try:
        p = os.path.join(_plan_dir(), "drafts", f"group_{gi}.json")
        if os.path.exists(p):
            with open(p, encoding="utf-8") as f:
                return json.load(f)
    except Exception as e:
        logger.warning(f"초안 로드 실패(무시): {e}")
    return None
```

(b) **자동 저장 훅 3곳**: `api_generate` task에서 `state["group_states"].setdefault(gi,{})["drafts"] = dr` 다음 줄에 `_save_drafts(gi)`. `api_generate_all` task의 같은 줄 다음에도 `_save_drafts(gi)`. `api_revise` task의 `drafts[di] = new_post` 다음에 `_save_drafts(gi)`.

(c) **복원 훅**: `api_drafts`(GET /api/drafts/<gi>)에서 `gs = state["group_states"].get(gi,{})` 다음에:
```python
    if not gs.get("drafts"):
        saved = _load_saved_drafts(gi)
        if saved:
            state["group_states"].setdefault(gi, {})["drafts"] = saved
            gs = state["group_states"][gi]
```
`api_blocks`에도 동일 복원 훅을 넣고, photos 폴백을 추가 — 현재:
```python
    photos = groups[gi].get("photos", []) if gi < len(groups) else []
```
→
```python
    photos = groups[gi].get("photos", []) if gi < len(groups) else []
    if not photos:
        photos = d.get("photo_results", [])   # 서버 재시작 후 복원 경로(base64 이미지 매칭용)
```
(`d = drafts[di]` 가 이미 위에 있음 — 순서 주의: photos 줄이 `d` 할당보다 뒤에 오도록.)

(d) **수동 저장 라우트** (`/api/drafts/<int:gi>` 라우트 앞에):
```python
@app.route('/api/drafts/save', methods=['POST'])
def api_drafts_save():
    saved = []
    for gi, gs in state["group_states"].items():
        if gs.get("drafts"):
            _save_drafts(gi)
            saved.append(gi)
    return jsonify({"saved_groups": saved})
```

(e) `/api/status` 응답 dict에 `"has_saved_drafts": os.path.isdir(os.path.join(_plan_dir(), "drafts")) if state.get("plan") else False,` 추가.

- [ ] **Step 4: full suite + import** → 54 passed, `import app` ok
- [ ] **Step 5: commit** — `feat(drafts): 초안 파일 영구 저장·자동 복원(G1) — 재시작 후에도 편집·발행 가능`

---

### Task 5 (G3): 인라인 블록 편집 (UI)

**Files:** Modify `static/index.html` — `renderS4` 블록 렌더부

- [ ] **Step 1: implement.** `renderS4`의 블록 아이템 렌더(현재):

```js
blocks.forEach((b,i)=>{const s=i===selBlock;
let pv='';if(b.type==='text')pv=(b.content||'').substring(0,50);else if(b.type==='image')pv='(이미지)';else if(b.type==='separator')pv='─ ─ ─';else pv=b.content||'';
h+=`<div class="block-item ${s?'selected':''}" onclick="selBlk(${i})"><div class="bt">${icons[b.type]||'?'} ${b.type}</div><div class="bc">${esc(pv)}</div><div class="ba"><button class="ba-btn" onclick="event.stopPropagation();mvBlk(${i},-1)">↑</button><button class="ba-btn" onclick="event.stopPropagation();mvBlk(${i},1)">↓</button><button class="ba-btn" onclick="event.stopPropagation();delBlk(${i})">×</button></div></div>`});
h+='</div>';
```

을 아래로 교체 (선택된 편집 가능 블록은 **그 자리에서** 큰 textarea):

```js
const EDITABLE=['text','styled_label','heading','route'];
blocks.forEach((b,i)=>{const s=i===selBlock;
let pv='';if(b.type==='text')pv=(b.content||'').substring(0,50);else if(b.type==='image')pv='(이미지)';else if(b.type==='separator')pv='─ ─ ─';else pv=b.content||'';
const inline=(s&&EDITABLE.includes(b.type))
  ?`<textarea id="bEdit" style="width:100%;min-height:120px;margin-top:6px" oninput="editBlk(this.value)" onclick="event.stopPropagation()">${esc(b.content||'')}</textarea>`
  :`<div class="bc">${esc(pv)}</div>`;
h+=`<div class="block-item ${s?'selected':''}" onclick="selBlk(${i})"><div class="bt">${icons[b.type]||'?'} ${b.type}</div>${inline}<div class="ba"><button class="ba-btn" onclick="event.stopPropagation();mvBlk(${i},-1)">↑</button><button class="ba-btn" onclick="event.stopPropagation();mvBlk(${i},1)">↓</button><button class="ba-btn" onclick="event.stopPropagation();delBlk(${i})">×</button></div></div>`});
h+='</div>';
```

그리고 그 아래의 기존 **하단 편집 카드 블록을 통째로 제거**:

```js
if(selBlock>=0&&selBlock<blocks.length){const b=blocks[selBlock];
if(['text','styled_label','heading','route'].includes(b.type)){
h+=`<div class="card mt-8"><div class="card-label">블록 #${selBlock+1} 편집</div><textarea id="bEdit" style="min-height:60px" oninput="editBlk(this.value)">${esc(b.content||'')}</textarea></div>`}}
```

주의: `selBlk(i)`는 이미 선택된 블록을 다시 클릭해도 renderS4를 다시 그림 → textarea 클릭에 `event.stopPropagation()`이 있어 입력 중 재렌더는 발생하지 않는다. `editBlk`는 기존 함수 재사용(변경 없음).

- [ ] **Step 2: parse check** — `node vm.Script` 원라이너 → `PARSE OK`
- [ ] **Step 3: commit** — `feat(edit): 블록 인라인 편집(G3) — 클릭한 자리에서 큰 입력창으로 바로 수정`

---

### Task 6 (G1 버튼 + G2): STEP4 저장 버튼 + STEP6 일차 탭·다음 일차

**Files:** Modify `static/index.html` — `renderS3`, `renderS5`, `doPub`

- [ ] **Step 1: STEP4 저장 버튼.** `renderS3`의 생성 버튼 flex div:

```js
<div class="flex gap-6 mt-12">
  <button class="btn btn-pri" style="flex:1" onclick="doGen()">✨ 선택한 Day만 생성</button>
  <button class="btn btn-ok" style="flex:1" onclick="doGenAll()">🚀 전체 일정 생성</button>
</div>`;
```

바로 뒤(백틱 닫기 전)에 추가:

```js
<button class="btn btn-ghost w-full mt-6" onclick="doSaveDrafts()">💾 초안 저장 (재시작해도 유지)</button>`;
```

그리고 `renderS3` 함수 뒤에:

```js
async function doSaveDrafts(){try{const r=await fetch(`${API}/api/drafts/save`,{method:'POST'});const d=await r.json();const n=(d.saved_groups||[]).length;alert(n?`✅ ${n}개 일차의 초안을 저장했습니다`:'저장할 초안이 없습니다 (먼저 생성해주세요)')}catch(e){alert('저장 실패: '+e.message)}}
```

- [ ] **Step 2: STEP6 일차 탭 + 발행 완료 표시.** 전역 어딘가(131행 `let tripTitle=...` 근처)에 `let publishedGis=new Set();` 추가. `renderS5`를 아래로 교체:

```js
function renderS5(){$et.textContent='🚀 발행';$badge.textContent='STEP 6';
const api=settings.api||{};
let tabs='';
if(groups.length>1){tabs=`<div class="group-tabs">`+groups.map((g,i)=>`<button class="group-tab ${i===curGroup?'active':''}" onclick="pubSwitch(${i})">${publishedGis.has(i)?'✓ ':''}${g?.label||'그룹'+(i+1)}</button>`).join('')+`</div>`}
$eb.innerHTML=`${tabs}
<div class="card"><div class="card-label">제목</div><input type="text" id="pTitle" value="${esc(draftTitle)}"></div>
<div class="card"><div class="card-label">태그</div><input type="text" id="pTags" value="${(draftTags||[]).join(', ')}" placeholder="쉼표 구분"></div>
<div class="card"><div class="card-label">발행 방식</div><select id="pMethod"><option value="selenium" ${api.publish_method==='selenium'?'selected':''}>자동 (Selenium)</option><option value="clipboard" ${api.publish_method==='clipboard'?'selected':''}>수동 (클립보드)</option></select></div>
<div class="card"><div class="card-label">공개 설정</div><select id="pVis"><option ${api.default_visibility==='비공개'?'selected':''}>비공개</option><option ${api.default_visibility==='공개'?'selected':''}>공개</option></select></div>
<div class="card"><div class="card-label">요약</div><div class="text-sm text-fg2">${groups[curGroup]?.label||''} · 블록 ${blocks.length}개 · 이미지 ${blocks.filter(b=>b.type==='image').length}장</div></div>
<button class="btn btn-pri w-full mt-12" onclick="doPub()">📤 이 일차 발행</button>
<button class="btn btn-ok w-full mt-6" onclick="doPubAll()">🚀 전체 일정 한번에 발행</button>
<button class="btn btn-ghost w-full mt-6" onclick="doSave()">💾 로컬 저장</button>`}

async function pubSwitch(gi){if(gi===curGroup){renderS5();return}
const r=await fetch(`${API}/api/blocks/${gi}/0`);const d=await r.json();
if(d.error||!d.blocks){alert(`${gi+1}일차의 초안이 없습니다. STEP4에서 생성해주세요.`);return}
curGroup=gi;blocks=d.blocks;draftTitle=d.title||draftTitle;draftTags=d.tags||[];renderS5();updatePreview()}
```

(주의: 기존 renderS5의 `<\div>`, `${API}\api\publish` 같은 역슬래시 오타가 원본에 있으면 그대로 두지 말고 이 교체본으로 정리한다. `doPub`의 fetch URL도 `${API}/api/publish`로 확인.)

- [ ] **Step 3: 발행 완료 → 다음 일차 흐름.** `doPub`의 완료 콜백:

```js
startPoll(p=>{if(p.status==='done')alert('✅ 발행 완료!');else if(p.status==='error')alert('❌ 발행 실패\n\n'+(p.message||'알 수 없는 오류'))})}
```

을 교체:

```js
startPoll(p=>{if(p.status==='done'){publishedGis.add(curGroup);
const next=groups.findIndex((g,i)=>!publishedGis.has(i));
if(next>=0&&confirm(`✅ 발행 완료!\n\n다음 일차(${groups[next]?.label||next+1+'일차'})를 이어서 발행할까요?`)){pubSwitch(next)}
else{alert('✅ 발행 완료!');renderS5()}}
else if(p.status==='error')alert('❌ 발행 실패\n\n'+(p.message||'알 수 없는 오류'))})}
```

- [ ] **Step 4: parse check + 렌더 확인** — `node vm.Script` → `PARSE OK`, 서버 재시작 후 `/browse` 콘솔 무에러.
- [ ] **Step 5: commit** — `feat(ui): STEP4 초안 저장 버튼 + STEP6 일차 탭·발행 후 다음 일차 흐름(G1/G2)`

---

## 최종 검증

- [ ] `venv/Scripts/python.exe -m pytest tests/test_quality.py -q` → 54 passed
- [ ] `venv/Scripts/python.exe -c "import core, app; print('ok')"` → ok
- [ ] `PARSE OK` + `/browse` 각 스텝 콘솔 무에러
- [ ] 실생성 E2E 1회: 최적화 제목(지역명 포함) 적용·original_title 보관, 위키박스 팁/가는법, 초안 파일 생성(`plans/*/drafts/group_0.json`), 서버 재시작 → STEP5 진입 시 초안 자동 복원 → 블록 정상

## Self-Review 결과

- **Spec 커버리지:** G1=T4+T6(버튼), G2=T6, G3=T5, G4a=T1, G4b=T2, G6=T3. 스펙 테스트 4종 전부 대응(G1 테스트는 tmp_path round trip).
- **Placeholder:** 없음 — 모든 스텝에 실제 코드.
- **타입 일관성:** `optimize_title(post, na, region)->str|None`(T3 정의=배선 일치), `_save_drafts(gi)`/`_load_saved_drafts(gi)->list|None`(T4 내 일관), `place_directives` dict가 TripPlanner→group→generate_drafts→_build_prompt로 동일 키. UI 전역 `publishedGis`는 T6에서 선언·사용.
