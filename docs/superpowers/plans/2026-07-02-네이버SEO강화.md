# 네이버 SEO·핵심 태그 강화 (E) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 네이버 상위 블로그 분석을 10개로 확장하고, 핵심 태그를 코드가 추출·병합(강제)하며, 생성 결과를 SEO 검증해 경고를 UI에 노출한다.

**Architecture:** 기존 `분석(NaverBlogAnalyzer)→프롬프트 참고(_naver_context)` 흐름에 코드 강제 계층 추가: 초안 생성 직후 `extract_core_tags`+`merge_tags`로 태그 보장, `seo_check`로 경고 생성(`post["seo_warnings"]`), 편집 화면(renderS4)에 배지 표시. `_naver_context`의 "지도 삽입 필수" 불변식 충돌 문구 제거. 신규 로직은 전부 순수 staticmethod(네트워크 없음, LLM 없음)로 유닛테스트.

**Tech Stack:** Python 3, Flask, pytest. 프론트는 `static/index.html` 인라인 스크립트(수정 후 `node vm.Script` 파싱 + `/browse` 렌더 확인 필수).

**스펙 대비 편차(추적용):** ① 스펙 §4의 `seo_check(post, analysis)`에 **`region` 인자 추가** — 제목/태그의 지역명 검사에 지역명이 필요한데 post dict에 region이 없고 `generate_drafts` 스코프의 `region` 변수가 정확한 소스이기 때문. ② 스펙 §4 "STEP4 초안 카드"의 실제 코드 위치는 **`renderS4`(편집 화면, step 5)의 초안 선택 영역** — 생성 직후 사용자가 초안을 처음 보는 화면이 여기다.

**기준 라인번호는 커밋 `d281997` 시점.** 앞 태스크 진행으로 밀릴 수 있으니 라인이 안 맞으면 함수명/문자열로 찾는다.

---

## File Structure

- `core.py` — `NaverBlogAnalyzer.analyze` count 확장 + `_extract_keywords` staticmethod 추출·정제(Task 1), `TravelBlogGenerator.extract_core_tags`/`merge_tags`/`seo_check` 신규(Task 2~4), `_naver_context` 문구 수정(Task 5), `generate_drafts` 배선(Task 6).
- `static/index.html` — `renderS4` 경고 배지(Task 7).
- `tests/test_quality.py` — 테스트 5개 추가 (38 → 43 passed).

---

### Task 1: `_extract_keywords` 추출·정제 + 분석 10개 확장

**Files:**
- Modify: `core.py:240` (`NaverBlogAnalyzer.analyze` — 시그니처, `blog_items[:3]`, 인라인 빈도 로직)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

`tests/test_quality.py`의 import 라인에 `NaverBlogAnalyzer`가 없으면 추가(`from core import ...` 기존 라인에 병기)하고, 파일 끝에 추가:

```python
def test_extract_keywords_stopwords():
    titles = ["후쿠오카 여행 갔다왔어요 진짜 좋았어요", "후쿠오카 맛집 라멘 후기 정말 추천"]
    descs = ["오늘 다녀온 후쿠오카 텐진 맛집 그리고 라멘"]
    kws = NaverBlogAnalyzer._extract_keywords(titles, descs, "후쿠오카 여행")
    assert "라멘" in kws and "맛집" in kws        # 실질 키워드는 살아남음
    assert "후쿠오카" not in kws                   # 검색 키워드 자신(부분 포함) 제외
    assert "진짜" not in kws and "정말" not in kws  # 불용어 제외
    assert "그리고" not in kws and "오늘" not in kws
    assert len(kws) <= 15
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_extract_keywords_stopwords -q`
Expected: FAIL (`_extract_keywords` 미정의 → AttributeError)

- [ ] **Step 3: Implement**

(a) `core.py:240`의 시그니처를 `def analyze(self, keyword, count=5):` → `def analyze(self, keyword, count=10):` 로 변경.

(b) `core.py:255`의 `for item in blog_items[:3]:` → `for item in blog_items[:count]:` 로 변경. 그 위의 주석 `# 2단계: 상위 블로그 본문 구조 분석 (최대 3개)`도 `(최대 count개)`로 수정.

(c) `analyze` 안의 인라인 키워드 빈도 블록(현재 이 형태):

```python
        # 키워드 분석
        all_text = ' '.join(titles + descs)
        words = re.findall(r'[가-힣a-zA-Z]{2,}', all_text)
        freq = {}
        for w in words:
            if w != keyword and w not in keyword and len(w) >= 2:
                freq[w] = freq.get(w, 0) + 1
        common = sorted(freq.items(), key=lambda x: x[1], reverse=True)[:15]
```

를 아래 한 줄로 교체(이후 `common_keywords` 사용부는 `[w[0] for w in common]` → `common` 으로 바꿈):

```python
        # 키워드 분석 (불용어·검색어 자신 제외)
        common = self._extract_keywords(titles, descs, keyword)
```

그리고 `result` dict의 `"common_keywords": [w[0] for w in common],` 를 `"common_keywords": common,` 으로 교체.

(d) `NaverBlogAnalyzer` 클래스 안(예: `_search_blogs` 앞)에 staticmethod 추가:

```python
    # 키워드 추출에서 걸러낼 일반어(여행 후기 상투어·부사·접속사)
    KEYWORD_STOPWORDS = {
        "있는", "있어요", "있었어요", "했어요", "하는", "합니다", "해서", "하고",
        "그리고", "그래서", "하지만", "진짜", "정말", "완전", "너무", "조금",
        "바로", "여기", "저희", "제가", "가장", "후기", "블로그", "오늘",
        "다녀왔어요", "다녀온", "갔다왔어요", "같아요", "있습니다", "추천",
    }

    @staticmethod
    def _extract_keywords(titles, descs, keyword, top_n=15):
        """제목+요약에서 연관 키워드 빈도 추출. 불용어·검색어 자신 제외. 순수함수."""
        all_text = ' '.join(list(titles) + list(descs))
        words = re.findall(r'[가-힣a-zA-Z]{2,}', all_text)
        stop = NaverBlogAnalyzer.KEYWORD_STOPWORDS
        freq = {}
        for w in words:
            if w in stop:
                continue
            if w == keyword or w in keyword or keyword in w:
                continue
            # 검색 키워드의 각 어절(예: "후쿠오카", "여행")과 겹치면 제외
            if any(w == part or w in part or part in w for part in keyword.split()):
                continue
            freq[w] = freq.get(w, 0) + 1
        ranked = sorted(freq.items(), key=lambda x: x[1], reverse=True)
        return [w for w, _ in ranked[:top_n]]
```

- [ ] **Step 4: Run tests to verify pass + no regression**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 39 passed (38 + 1)

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(seo): 상위 블로그 방문 10개 확장 + 키워드 추출 불용어 정제(_extract_keywords)"
```

---

### Task 2: `extract_core_tags` — 핵심 태그 추출 (순수)

**Files:**
- Modify: `core.py` — `TravelBlogGenerator` 클래스에 staticmethod 추가 (`_naver_context` 메서드 앞이 좋은 위치)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_extract_core_tags():
    analysis = {"common_keywords": ["라멘", "맛집", "온천", "야경", "카페"]}
    tags = TravelBlogGenerator.extract_core_tags(analysis, "후쿠오카")
    assert "후쿠오카" in tags and "후쿠오카여행" in tags
    assert "후쿠오카맛집" in tags            # 맛집 신호 있음 → 조합 태그
    assert 3 <= len(tags) <= 5
    # analysis 없음 → region 최소 태그
    assert TravelBlogGenerator.extract_core_tags(None, "삿포로") == ["삿포로", "삿포로여행"]
    # 둘 다 없음 → 빈 리스트 (조용히 생략)
    assert TravelBlogGenerator.extract_core_tags(None, "") == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_extract_core_tags -q`
Expected: FAIL (AttributeError)

- [ ] **Step 3: Implement** — `TravelBlogGenerator` 클래스 안에 추가:

```python
    @staticmethod
    def extract_core_tags(analysis, region):
        """실측 데이터(상위 블로그 빈도 키워드)+확정 지역명으로 핵심 태그 3~5개.
        LLM 없음(결정적). analysis/region 없으면 축소·빈 리스트(조용히 생략)."""
        region = (region or "").strip()
        tags = []
        if region:
            tags.append(region)
            tags.append(f"{region}여행")
        kws = (analysis or {}).get("common_keywords") or []
        if region and any("맛집" in k for k in kws):
            tags.append(f"{region}맛집")
        for k in kws:
            if len(tags) >= 5:
                break
            if k and k not in tags:
                tags.append(k)
        return tags[:5]
```

- [ ] **Step 4: Run tests to verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 40 passed

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(seo): 핵심 태그 추출(extract_core_tags) — 실측 키워드+지역명, LLM 없음"
```

---

### Task 3: `merge_tags` — 핵심 우선 병합 (순수)

**Files:**
- Modify: `core.py` — `TravelBlogGenerator`에 staticmethod 추가 (`extract_core_tags` 바로 뒤)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_merge_tags():
    core = ["후쿠오카", "후쿠오카여행", "라멘"]
    ai = ["# 후쿠오카", "야타이", "텐진", "라멘 ", "야경", "온천", "신사", "공원", "카페", "쇼핑"]
    merged = TravelBlogGenerator.merge_tags(ai, core)
    assert merged[:3] == core                       # 핵심 태그 우선 배치
    assert len(merged) <= 10                        # 상한
    norm = [t.replace(" ", "").replace("#", "") for t in merged]
    assert len(norm) == len(set(norm))              # 정규화 기준 중복 없음("# 후쿠오카"/"라멘 " 제거됨)
    # 빈 입력 안전
    assert TravelBlogGenerator.merge_tags(None, []) == []
    assert TravelBlogGenerator.merge_tags(["a"], None) == ["a"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_merge_tags -q`
Expected: FAIL (AttributeError)

- [ ] **Step 3: Implement**

```python
    @staticmethod
    def merge_tags(ai_tags, core_tags, cap=10):
        """핵심 태그(코드 추출)를 앞에 보장하고 AI 태그로 채움.
        공백/# 무시 정규화로 중복 제거, 상한 cap."""
        def norm(t):
            return re.sub(r'[\s#]', '', str(t)).lower()
        merged, seen = [], set()
        for t in list(core_tags or []) + list(ai_tags or []):
            t = str(t).strip()
            if not t:
                continue
            k = norm(t)
            if not k or k in seen:
                continue
            seen.add(k)
            merged.append(t)
            if len(merged) >= cap:
                break
        return merged
```

- [ ] **Step 4: Run tests to verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 41 passed

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(seo): 태그 병합(merge_tags) — 핵심 우선·정규화 중복제거·상한 10"
```

---

### Task 4: `seo_check` — 생성 후 SEO 검증 경고 (순수)

**Files:**
- Modify: `core.py` — `TravelBlogGenerator`에 staticmethod 추가 (`merge_tags` 바로 뒤)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_seo_check_warnings():
    analysis = {"common_keywords": ["라멘", "맛집"],
                "avg_analysis": {"avg_chars": 4000}}
    # 문제 3종: 제목에 지역명 없음 / 분량 60% 미만 / 태그에 지역명 없음
    post = {"title": "겨울 바다와 라멘 한 그릇",
            "content": "<p>" + ("가나다라마바사아" * 100) + "</p>",   # 800자 = 4000의 20%
            "tags": ["라멘", "야경"]}
    warns = TravelBlogGenerator.seo_check(post, analysis, "후쿠오카")
    assert len(warns) == 3
    assert any("제목" in w for w in warns)
    assert any("분량" in w for w in warns)
    assert any("태그" in w for w in warns)
    # 문제 없음 → 빈 리스트
    good = {"title": "후쿠오카 여행 라멘 총정리",
            "content": "<p>" + ("가나다라마바사아" * 500) + "</p>",   # 4000자
            "tags": ["후쿠오카", "라멘"]}
    assert TravelBlogGenerator.seo_check(good, analysis, "후쿠오카") == []
    # analysis 없음 → 빈 리스트 (검증 스킵)
    assert TravelBlogGenerator.seo_check(post, None, "후쿠오카") == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_seo_check_warnings -q`
Expected: FAIL (AttributeError)

- [ ] **Step 3: Implement**

```python
    @staticmethod
    def seo_check(post, analysis, region=""):
        """생성 결과 SEO 검증 → 경고 문자열 목록(발행은 막지 않음).
        analysis 없으면 빈 리스트(검증 스킵). 순수함수."""
        if not analysis or not isinstance(analysis, dict):
            return []
        warns = []
        region = (region or "").strip()
        title = (post or {}).get("title", "") or ""
        kws = analysis.get("common_keywords") or []
        # 1) 제목에 핵심 키워드(지역명 또는 빈도 1위) 포함 여부
        head_kw = region or (kws[0] if kws else "")
        if head_kw and head_kw not in title:
            warns.append(f"제목에 핵심 키워드 '{head_kw}' 미포함")
        # 2) 본문 분량: 상위 평균의 60% 미만이면 경고
        avg_chars = (analysis.get("avg_analysis") or {}).get("avg_chars") or 0
        if avg_chars > 0:
            body = re.sub(r'<[^>]+>', '', (post or {}).get("content", "") or "")
            n = len(body.replace("\n", "").replace(" ", ""))
            if n < avg_chars * 0.6:
                warns.append(f"분량 {n:,}자 — 상위 평균({avg_chars:,}자)의 {n*100//avg_chars}%")
        # 3) 태그에 지역명 포함 여부
        if region:
            tags = (post or {}).get("tags") or []
            if not any(region in str(t) for t in tags):
                warns.append(f"태그에 지역명 '{region}' 부재")
        return warns
```

- [ ] **Step 4: Run tests to verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 42 passed

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(seo): 생성 후 SEO 검증(seo_check) — 제목 키워드·분량 60%·태그 지역명 경고"
```

---

### Task 5: `_naver_context` 지도 문구 결함 수정 (불변식)

**Files:**
- Modify: `core.py:3181` 근처 (`_naver_context` 내부)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_naver_context_no_map_directive():
    na = {"keyword": "후쿠오카 여행", "top_titles": ["t1"], "common_keywords": ["라멘"],
          "avg_analysis": {"avg_chars": 4000, "avg_images": 15, "avg_videos": 0,
                           "map_ratio": 0.8, "common_sections": []}}
    g = TravelBlogGenerator.__new__(TravelBlogGenerator)
    ctx = TravelBlogGenerator._naver_context(g, na)
    assert "지도 삽입 필수" not in ctx           # 불변식 충돌 문구 제거됨
    assert "넣지" in ctx                          # "본문에 절대 넣지 말 것" 안내로 대체
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_naver_context_no_map_directive -q`
Expected: FAIL ("지도 삽입 필수"가 포함되어 assert 실패)

- [ ] **Step 3: Fix** — `_naver_context` 안의 이 블록:

```python
            mr = avg.get("map_ratio", 0)
            if mr >= 0.3:
                parts.append(f"  지도: {mr*100:.0f}% 포함 → 지도 삽입 필수")
```

를 아래로 교체:

```python
            mr = avg.get("map_ratio", 0)
            if mr >= 0.3:
                parts.append(f"  지도: 상위 글 {mr*100:.0f}%가 포함 — 지도·경로는 코드가 자동 삽입하니 본문에 절대 넣지 마세요")
```

- [ ] **Step 4: Run tests to verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 43 passed

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "fix(seo): _naver_context 지도 삽입 유도 문구 제거 — 형식은 코드가 조립 불변식"
```

---

### Task 6: `generate_drafts` 배선 — 태그 병합·경고 부착

**Files:**
- Modify: `core.py` — `generate_drafts`의 초안 완성 블록 (현재 아래 형태, `post["photo_results"] = photos` 라인 검색)

- [ ] **Step 1: Wire in.** 현재 코드:

```python
            post = self._call_ai(prompt, s["temp"], photos)
            if post:
                post["style"] = s["name"]
                # 지역 위키 박스를 글 최상단에 삽입 (있을 때만)
                if region_desc and post.get("content"):
                    post["content"] = region_desc + "\n" + post["content"]
                post["region_desc"] = region_desc
                post["course_line"] = course_line
                post["group_label"] = group_label
                post["photo_results"] = photos
                drafts.append(post)
```

`post["photo_results"] = photos` 와 `drafts.append(post)` 사이에 3줄 삽입:

```python
                # SEO 강제·검증 (E): 핵심 태그 병합 + 경고 부착
                post["tags"] = self.merge_tags(post.get("tags"), self.extract_core_tags(naver_analysis, region))
                post["seo_warnings"] = self.seo_check(post, naver_analysis, region)
```

(`naver_analysis`는 `generate_drafts`의 기존 인자, `region`은 같은 함수 상단에서 `self._koreanize_region(...)` 결과로 이미 존재하는 지역명 변수다.)

- [ ] **Step 2: Full suite regression**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: 43 passed (기존 생성 스모크 테스트 포함 회귀 없음. `naver_analysis=None`이면 `extract_core_tags`가 region 태그만, `seo_check`가 `[]` 반환 — 안전)

- [ ] **Step 3: Import check**

Run: `venv/Scripts/python.exe -c "import core, app; print('ok')"`
Expected: `ok`

- [ ] **Step 4: Commit**

```bash
git add core.py
git commit -m "feat(seo): generate_drafts에 핵심 태그 병합·seo_warnings 부착 배선"
```

---

### Task 7: UI — 편집 화면에 SEO 경고 배지

**Files:**
- Modify: `static/index.html` — `renderS4` 함수 (초안 선택 블록과 제목 카드 사이, 현재 744~745행 부근)

- [ ] **Step 1: Insert warning card.** `renderS4` 안의 이 두 줄 사이:

```js
if(drafts.length>1){h+=`<div class="flex gap-6 mb-8">`+drafts.map((d,i)=>`<button class="btn ${i===curDraft?'btn-pri':'btn-ghost'} btn-sm" onclick="loadBlocks(${i})">초안${i+1}</button>`).join('')+`</div>`}
h+=`<div class="card"><div class="card-label">제목</div><input type="text" id="bTitle" value="${esc(draftTitle)}" oninput="draftTitle=this.value;updatePreview()"></div>`;
```

사이에 아래 한 줄 삽입:

```js
const sw=(drafts[curDraft]||{}).seo_warnings||[];if(sw.length){h+=`<div class="card" style="background:#FDF3E7;border-color:#EED9B8"><div class="card-label">⚠️ SEO 점검</div>${sw.map(x=>`<div class="text-xs" style="color:#8A6D1F;margin:2px 0">· ${esc(x)}</div>`).join('')}</div>`}
```

- [ ] **Step 2: Validate inline script parses** (구문오류 1개 = SPA 전체 사망)

Run:
```bash
node -e "const fs=require('fs');const html=fs.readFileSync('static/index.html','utf8');const m=[...html.matchAll(/<script>([\s\S]*?)<\/script>/g)];let ok=true;m.forEach((s,i)=>{try{new(require('vm').Script)(s[1]);}catch(e){ok=false;console.log('script#'+i,e.message);}});console.log(ok?'PARSE OK':'PARSE FAIL');"
```
Expected: `PARSE OK`

- [ ] **Step 3: Browser render check** — 서버 재시작 후 `/browse`로 확인:

```bash
B="$HOME/.claude/skills/gstack/browse/dist/browse"
"$B" goto http://localhost:5000/ >/dev/null 2>&1
"$B" console --errors
```
Expected: `(no console errors)` + 페이지 텍스트에 6단계 스텝바 렌더

- [ ] **Step 4: Commit**

```bash
git add static/index.html
git commit -m "feat(seo): 편집 화면에 SEO 점검 경고 배지(renderS4)"
```

---

## 최종 검증 (전체)

- [ ] `venv/Scripts/python.exe -m pytest tests/test_quality.py -q` → 43 passed
- [ ] `venv/Scripts/python.exe -c "import core, app; print('ok')"` → ok
- [ ] 인라인 스크립트 PARSE OK + `/browse` 콘솔 무에러
- [ ] (선택, 키·네트워크 있을 때) 실제 흐름: `/api/seo` 실행 → 생성 → 초안의 `tags`에 지역명 태그 선두 포함 + `seo_warnings` 동작 확인

---

## Self-Review 결과

- **Spec 커버리지:** §4 컴포넌트 — analyze 확장·`_extract_keywords`(Task 1), `extract_core_tags`(Task 2), `merge_tags`(Task 3), `seo_check`(Task 4), `_naver_context` 수정(Task 5), `generate_drafts` 배선(Task 6), UI 배지(Task 7). §7 테스트 5개 전부 대응. §6 에러처리 — analysis 부재 폴백은 Task 2/4의 코드+테스트로, opt-in 격리는 무변경으로 충족. 편차 2건(seo_check region 인자, UI 위치)은 헤더에 명시.
- **Placeholder:** 없음. 모든 코드 스텝에 실제 코드 포함.
- **타입 일관성:** `extract_core_tags(analysis, region) -> list[str]` / `merge_tags(ai_tags, core_tags, cap=10) -> list` / `seo_check(post, analysis, region="") -> list[str]` 시그니처가 Task 2~4 정의와 Task 6 호출부에서 일치. `common_keywords`는 Task 1 이후에도 `list[str]`(기존 스키마 유지)로 Task 2/4 소비부와 일치.
