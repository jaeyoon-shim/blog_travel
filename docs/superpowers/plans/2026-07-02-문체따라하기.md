# 문체 따라하기 (Style Mimicry) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 참고 블로그 문체를 LLM으로 규칙 JSON으로 추출하고, 원문 통째가 아니라 "규칙 + 짧은 예시"로 생성 프롬프트에 주입한다.

**Architecture:** 기존 `StyleAnalyzer.analyze()`를 `_scrape`(스크래핑, 기존 로직) → `extract_profile`(LLM 또는 정규식 폴백)로 분리한다. 결과 문체 프로파일(dict)을 기존 `state["style_analysis"]` 슬롯에 저장(=generate 배선 그대로 재사용). 생성 시 `TravelBlogGenerator._style_context(profile)`가 규칙+예시 프롬프트 조각을 조립한다.

**Tech Stack:** Python 3, Flask, OpenAI SDK(`response_format=json_object`, 기존 패턴 재사용), BeautifulSoup, pytest + monkeypatch.

**설계 대비 편차(추적용):** 스펙 §5는 새 키 `state["style_profile"]`를 명시했으나, `generate_drafts`가 이미 `state["style_analysis"]`를 받아 `_style_context`로 넘기므로(app.py:707,743 / core.py:1825) **기존 슬롯을 재사용**한다. 새 키·시그니처 변경을 피해 회귀 위험을 줄이기 위함. 슬롯의 값 스키마만 (구)analysis → (신)profile로 바뀐다. `/api/style`는 항상 새 프로파일을 쓰므로 `_style_context`는 새 스키마만 읽으면 된다.

---

## File Structure

- `core.py` — `StyleAnalyzer` 확장(`__init__`, `_scrape`, `extract_profile`, `_extract_profile_llm`, `_normalize_profile`, `_profile_from_regex`, `analyze` 개선), `TravelBlogGenerator._style_context` 개선. 기존 `_detect_tone`/`_detect_endings`는 폴백에서 재사용.
- `app.py` — `/api/style`가 `StyleAnalyzer(state["cfg"])` 사용 + `warning` 응답 필드.
- `static/index.html` — `doStyle()`가 warning/추출 톤을 상태줄에 표시.
- `tests/test_quality.py` — 순수함수/라우팅 유닛테스트 6개(네트워크 없음).

---

### Task 1: `_style_context(profile)` — 규칙+예시 프롬프트 조각 (순수함수)

**Files:**
- Modify: `core.py:3136-3139` (`TravelBlogGenerator._style_context`)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing tests**

`tests/test_quality.py` 상단 import 근처에 `from core import TravelBlogGenerator` 가 없으면 추가하고, 파일 끝에 추가:

```python
def _style_ctx(profile):
    # _style_context는 self를 쓰지 않는 순수 로직 → 언바운드 호출로 테스트
    return TravelBlogGenerator._style_context(TravelBlogGenerator.__new__(TravelBlogGenerator), profile)


def test_style_context_from_profile():
    profile = {
        "tone": "친근 구어체",
        "person": "1인칭 혼잣말체",
        "sentence_length": "짧고 끊어치는 리듬",
        "ending_patterns": ["~더라고요", "~었어요"],
        "emoji_usage": "ㅎㅎ 가볍게 문단당 1회",
        "rhetorical_habits": ["'근데'로 화제 전환"],
        "donts": ["격식체 금지"],
        "examples": ["아 이거지 싶더라고요", "기다린 보람 있었어요!"],
        # 표절 방지 회귀용: 원문 문단은 절대 조각에 실리면 안 됨
        "sample_paragraphs": ["이것은매우긴원문문단으로절대프롬프트에통째로들어가면안되는내용ABCDEF"],
    }
    ctx = _style_ctx(profile)
    assert "친근 구어체" in ctx
    assert "~더라고요" in ctx
    assert "아 이거지 싶더라고요" in ctx
    # 원문 문단 통째 주입 금지(표절 방지)
    assert "절대프롬프트에통째로들어가면안되는내용ABCDEF" not in ctx
    # 예시는 최대 2개
    assert ctx.count('"') == 4


def test_style_context_empty():
    assert _style_ctx(None) == ""
    assert _style_ctx({}) == ""
    assert _style_ctx({"tone": ""}) == ""
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_style_context_from_profile tests/test_quality.py::test_style_context_empty -q`
Expected: FAIL (현재 `_style_context`는 `common_endings`/`sample_paragraphs`(구 스키마)를 읽고 예시 상한이 없어 assert 불일치).

- [ ] **Step 3: Rewrite `_style_context`**

`core.py:3136-3139`의 기존 메서드를 아래로 교체:

```python
    def _style_context(self, profile):
        """문체 프로파일(dict) → 프롬프트 조각. 순수함수, 네트워크 없음.
        규칙 + 짧은 예시만 넣고 원문 문단은 넣지 않는다(표절 방지)."""
        if not profile or not isinstance(profile, dict):
            return ""
        tone = (profile.get("tone") or "").strip()
        if not tone:
            return ""
        lines = ["\n[참고 문체 — 아래 규칙을 모방하되 문장을 그대로 베끼지 말 것]"]
        person = (profile.get("person") or "").strip()
        lines.append(f"· 톤: {tone}" + (f" / {person}" if person else ""))
        if profile.get("sentence_length"):
            lines.append(f"· 리듬: {profile['sentence_length']}")
        endings = [e for e in (profile.get("ending_patterns") or []) if e][:5]
        if endings:
            lines.append("· 어미: " + " / ".join(endings))
        if profile.get("emoji_usage"):
            lines.append(f"· 이모지: {profile['emoji_usage']}")
        habits = [h for h in (profile.get("rhetorical_habits") or []) if h][:5]
        if habits:
            lines.append("· 습관: " + ", ".join(habits))
        donts = [d for d in (profile.get("donts") or []) if d][:3]
        if donts:
            lines.append("· 피할 것: " + ", ".join(donts))
        examples = [e for e in (profile.get("examples") or []) if e][:2]
        if examples:
            lines.append("· 참고 어감(복붙 금지): " + " / ".join(f'"{e}"' for e in examples))
        return "\n".join(lines) + "\n"
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_style_context_from_profile tests/test_quality.py::test_style_context_empty -q`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(style): _style_context를 규칙+예시 프롬프트 조각으로 재작성(표절 방지 상한)"
```

---

### Task 2: `StyleAnalyzer.__init__` + 정규식 폴백 프로파일

**Files:**
- Modify: `core.py:70-72` (`StyleAnalyzer` 클래스 시작, `__init__` 추가)
- Modify: `core.py` (`StyleAnalyzer` 안에 `extract_profile` + `_profile_from_regex` 추가)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

`tests/test_quality.py`에 `from core import StyleAnalyzer` 추가하고 테스트 추가:

```python
def test_extract_profile_regex_fallback():
    sa = StyleAnalyzer(None)  # config 없음 → 정규식 폴백
    p = sa.extract_profile("첫날은 살살 돌았어요. 라멘이 진짜 맛있더라고요. 웨이팅 있었어요.")
    assert p["extracted_by"] == "regex_fallback"
    assert p["tone"]                       # 톤이 채워짐
    assert isinstance(p["ending_patterns"], list)
    assert isinstance(p["examples"], list)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_extract_profile_regex_fallback -q`
Expected: FAIL (`StyleAnalyzer()` 는 config 인자를 안 받고 `extract_profile` 미정의 → TypeError/AttributeError).

- [ ] **Step 3: Add `__init__`, `extract_profile`, `_profile_from_regex`**

`core.py:70-72`의 클래스 헤더를 아래로 교체(독스트링 다음에 `__init__` 삽입):

```python
class StyleAnalyzer:
    """참고 블로그 URL의 문체, 구조, 톤을 분석"""

    def __init__(self, config=None):
        self.config = config
```

그리고 `StyleAnalyzer` 안(예: `_empty` 메서드 앞, `core.py:154` 근처)에 추가:

```python
    def extract_profile(self, text, headings=None):
        """원문 텍스트 → 문체 규칙 프로파일(dict).
        config가 유효하면 LLM 추출, 아니면 정규식 폴백."""
        cfg = self.config
        if cfg is not None and hasattr(cfg, "is_valid") and cfg.is_valid():
            prof = self._extract_profile_llm(text)
            if prof:
                return prof
        return self._profile_from_regex(text)

    def _profile_from_regex(self, text):
        """LLM 없이 기존 정규식 휴리스틱으로 최소 프로파일 구성."""
        return {
            "tone": self._detect_tone(text),
            "sentence_length": "",
            "ending_patterns": self._detect_endings(text),
            "emoji_usage": "",
            "rhetorical_habits": [],
            "person": "",
            "dos": [], "donts": [],
            "examples": [],
            "extracted_by": "regex_fallback",
            "warning": None,
        }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_extract_profile_regex_fallback -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(style): StyleAnalyzer config 주입 + 정규식 폴백 프로파일(extract_profile)"
```

---

### Task 3: LLM 문체 추출 + 정규화 (`_extract_profile_llm`, `_normalize_profile`)

**Files:**
- Modify: `core.py` (`StyleAnalyzer` 안, Task 2에서 추가한 메서드들 뒤)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing tests**

```python
def test_normalize_profile_caps():
    raw = {
        "tone": "  친근  ",
        "ending_patterns": ["~요", "~죠", "~네요", "~더라", "~군", "~구나", "~잖아"],  # 7개
        "examples": ["문장1", "문장2", "문장3"],   # 3개
        "rhetorical_habits": "질문형 도입",          # 문자열도 허용
    }
    p = StyleAnalyzer._normalize_profile(raw)
    assert p["tone"] == "친근"                       # strip
    assert len(p["ending_patterns"]) == 6            # 6개로 캡
    assert len(p["examples"]) == 2                   # 2개로 캡(표절 방지)
    assert p["rhetorical_habits"] == ["질문형 도입"]  # str→list
    assert p["extracted_by"] == "llm"


def test_extract_profile_uses_llm_when_valid(monkeypatch):
    class FakeCfg:
        def is_valid(self): return True
        def get(self, *a): return "sk-test"
    sa = StyleAnalyzer(FakeCfg())
    fixture = {"tone": "감성체", "ending_patterns": ["~네요"], "examples": ["예시"],
               "extracted_by": "llm", "warning": None}
    monkeypatch.setattr(sa, "_extract_profile_llm", lambda text: fixture)
    p = sa.extract_profile("아무 긴 텍스트 " * 10)
    assert p is fixture
    assert p["extracted_by"] == "llm"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_normalize_profile_caps tests/test_quality.py::test_extract_profile_uses_llm_when_valid -q`
Expected: FAIL (`_normalize_profile`/`_extract_profile_llm` 미정의).

- [ ] **Step 3: Add `_extract_profile_llm` and `_normalize_profile`**

`StyleAnalyzer` 안(Task 2 메서드 뒤)에 추가:

```python
    def _extract_profile_llm(self, text):
        """LLM으로 문체 규칙 JSON 추출. 실패 시 None(폴백 유도)."""
        if not text or len(text) < 30:
            return None
        try:
            from openai import OpenAI
            client = OpenAI(api_key=self.config.get("openai", "api_key"))
            model = self.config.get("openai", "model") or "gpt-4o-mini"
            prompt = (
                "다음은 한 블로거의 글이다. 이 사람의 '문체'만 분석해 JSON으로 출력하라. "
                "내용/사실이 아니라 말투·리듬·습관만 본다. 확실한 것만, 없으면 빈 값/빈 배열. "
                "키: tone(문장 톤 한 줄), sentence_length(문장 길이·리듬 한 줄), "
                "ending_patterns(자주 쓰는 어미 배열, 예 '~더라고요'), "
                "emoji_usage(이모지/초성 사용 습관 한 줄), "
                "rhetorical_habits(수사·도입·전환 습관 배열), person(화자 시점 한 줄), "
                "dos(살릴 특징 배열), donts(피할 것 배열), "
                "examples(문체가 잘 드러나는 짧은 문장 1~2개 배열, 한 문장씩).\n\n"
                f"[글]\n{text[:4000]}"
            )
            r = client.chat.completions.create(
                model=model,
                messages=[{"role": "user", "content": prompt}],
                response_format={"type": "json_object"},
                temperature=0,
            )
            data = json.loads(r.choices[0].message.content)
        except Exception as e:
            logger.warning(f"⚠️ 문체 LLM 추출 실패: {e}")
            return None
        return self._normalize_profile(data)

    @staticmethod
    def _normalize_profile(data):
        """LLM 응답 dict를 안정 스키마로 정규화 + 상한 적용."""
        def _list(v, n):
            if isinstance(v, str):
                v = [v]
            return [str(x).strip() for x in (v or []) if str(x).strip()][:n]
        return {
            "tone": str(data.get("tone", "") or "").strip(),
            "sentence_length": str(data.get("sentence_length", "") or "").strip(),
            "ending_patterns": _list(data.get("ending_patterns"), 6),
            "emoji_usage": str(data.get("emoji_usage", "") or "").strip(),
            "rhetorical_habits": _list(data.get("rhetorical_habits"), 6),
            "person": str(data.get("person", "") or "").strip(),
            "dos": _list(data.get("dos"), 5),
            "donts": _list(data.get("donts"), 5),
            "examples": _list(data.get("examples"), 2),
            "extracted_by": "llm",
            "warning": None,
        }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_normalize_profile_caps tests/test_quality.py::test_extract_profile_uses_llm_when_valid -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(style): LLM 문체 추출(_extract_profile_llm) + 스키마 정규화·상한(_normalize_profile)"
```

---

### Task 4: `analyze` 리팩터 — `_scrape` 분리 + 텍스트 부족 경고

**Files:**
- Modify: `core.py:73-127` (`analyze` 메서드)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_thin_text_warning(monkeypatch):
    sa = StyleAnalyzer(None)  # 정규식 폴백(네트워크 없음)
    monkeypatch.setattr(sa, "_scrape", lambda url: {
        "title": "t", "headings": [], "paragraphs": ["아주 짧은 글"], "image_count": 0})
    p = sa.analyze("http://example.com")
    assert p["warning"]                    # 200자 미만 → 경고 세팅
    assert p["source_urls"] == ["http://example.com"]
    assert p["extracted_by"] == "regex_fallback"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_thin_text_warning -q`
Expected: FAIL (`_scrape` 미정의; 현재 `analyze`는 구 스키마 dict 반환, `warning`/`source_urls` 없음).

- [ ] **Step 3: Refactor `analyze` into `_scrape` + profile assembly**

`core.py:73-127`의 기존 `analyze` 메서드 전체를 아래 두 메서드로 교체(스크래핑 로직은 기존 것을 `_scrape`로 이동):

```python
    def analyze(self, url):
        logger.info(f"📎 참고 블로그 분석: {url}")
        scraped = self._scrape(url)
        text = " ".join(scraped.get("paragraphs", []))
        profile = self.extract_profile(text, scraped.get("headings"))
        profile["source_urls"] = [url]
        profile["title"] = scraped.get("title", "")
        if len(text) < 200:
            profile["warning"] = "본문을 충분히 못 읽었습니다 — 문체 반영이 약할 수 있어요."
        logger.info(f"✅ 문체 분석 완료: {profile.get('tone','')} ({profile.get('extracted_by')})")
        return profile

    def _scrape(self, url):
        """URL에서 원문 문단/제목/이미지수 수집. 실패 시 빈 구조."""
        empty = {"title": "", "headings": [], "paragraphs": [], "image_count": 0}
        try:
            from bs4 import BeautifulSoup
        except ImportError:
            logger.warning("⚠️  pip install beautifulsoup4 필요")
            return empty
        try:
            req = urllib.request.Request(url, headers={
                "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0.0.0"})
            with urllib.request.urlopen(req, timeout=15) as r:
                html = r.read().decode('utf-8', errors='ignore')
            soup = BeautifulSoup(html, 'html.parser')
            title = soup.find('title').get_text(strip=True) if soup.find('title') else ""
            headings = []
            for tag in ['h2', 'h3', 'strong', 'b']:
                for h in soup.find_all(tag):
                    t = h.get_text(strip=True)
                    if t and 5 < len(t) < 80:
                        headings.append(t)
            headings = headings[:15]
            paras = []
            for p in soup.find_all(['p', 'div', 'span']):
                t = p.get_text(strip=True)
                if 30 < len(t) < 500:
                    paras.append(t)
            paras = list(dict.fromkeys(paras))[:20]
            img_count = len([i for i in soup.find_all('img') if i.get('src', '').startswith('http')])
            return {"title": title, "headings": headings,
                    "paragraphs": paras, "image_count": img_count}
        except Exception as e:
            logger.warning(f"⚠️  분석 실패: {e}")
            return empty
```

기존 `_detect_tone`/`_detect_endings`/`_empty`는 그대로 둔다(폴백에서 사용). `_empty`는 이제 미참조가 될 수 있으나 제거하지 않는다(다른 참조 여부 불확실 → YAGNI, 남겨도 무해).

- [ ] **Step 4: Run test to verify it passes**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_thin_text_warning -q`
Expected: PASS

- [ ] **Step 5: Run full suite (회귀 확인)**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py -q`
Expected: PASS (기존 32 + 신규 6 = 38 passed)

- [ ] **Step 6: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(style): analyze를 _scrape+프로파일 조립으로 리팩터, 텍스트 부족 시 경고"
```

---

### Task 5: `/api/style` 배선 — config 전달 + warning 응답

**Files:**
- Modify: `app.py:621-639` (`api_style`)

- [ ] **Step 1: Rewrite `/api/style` route**

`app.py:621-639`을 아래로 교체:

```python
@app.route('/api/style', methods=['POST'])
def api_style():
    data = request.json or {}
    urls = data.get("urls", [])
    if not urls:
        return jsonify({"error": "URL 필요"}), 400
    try:
        sa = StyleAnalyzer(state.get("cfg"))
        results = [sa.analyze(u) for u in urls]
        if results:
            merged = dict(results[0])
            all_end, all_ex, all_src = [], [], []
            for r in results:
                all_end.extend(r.get("ending_patterns", []))
                all_ex.extend(r.get("examples", []))
                all_src.extend(r.get("source_urls", []))
            merged["ending_patterns"] = list(dict.fromkeys(all_end))[:6]
            merged["examples"] = list(dict.fromkeys(all_ex))[:2]
            merged["source_urls"] = list(dict.fromkeys(all_src))
            state["style_analysis"] = merged
        warning = next((r.get("warning") for r in results if r.get("warning")), None)
        return jsonify({"count": len(results), "results": results, "warning": warning})
    except Exception as e:
        return jsonify({"error": str(e)}), 500
```

- [ ] **Step 2: Verify import + route load**

Run: `venv/Scripts/python.exe -c "import app; print('ok')"`
Expected: `ok` (StyleAnalyzer는 app.py:16에서 이미 import됨)

- [ ] **Step 3: Smoke test the route (network 없이 config 무효 → 폴백)**

`.env`의 `OPENAI_API_KEY`가 유효하면 실제 LLM을 타므로, 스모크는 무효 키 환경에서:

Run:
```bash
venv/Scripts/python.exe -c "
import app
app.init_engine()
c = app.app.test_client()
r = c.post('/api/style', json={'urls':['http://example.com']})
print(r.status_code, 'warning' in r.get_json())
"
```
Expected: `200 True` (example.com은 본문이 얇아 warning 세팅됨; 예외 없이 프로파일 생성).

- [ ] **Step 4: Commit**

```bash
git add app.py
git commit -m "feat(style): /api/style가 config 주입 StyleAnalyzer 사용 + warning 응답"
```

---

### Task 6: UI — `doStyle()` 경고/톤 표시

**Files:**
- Modify: `static/index.html:647` (`doStyle` 함수)

- [ ] **Step 1: Update `doStyle` success handling**

`static/index.html:647`의 `doStyle` 함수에서 성공 처리 부분을 교체. 기존:

```js
const d=await r.json();if(d.error)throw new Error(d.error);stat(`✅ ${refUrls.length}개 분석 완료`)
```

교체:

```js
const d=await r.json();if(d.error)throw new Error(d.error);const tone=(d.results&&d.results[0]&&d.results[0].tone)?` · ${d.results[0].tone}`:'';const warn=d.warning?` ⚠️ ${d.warning}`:'';stat(`✅ ${refUrls.length}개 분석 완료${tone}${warn}`)
```

- [ ] **Step 2: Validate inline script parses (구문오류 = SPA 전체 사망)**

Run:
```bash
node -e "const fs=require('fs');const html=fs.readFileSync('static/index.html','utf8');const m=[...html.matchAll(/<script>([\s\S]*?)<\/script>/g)];let ok=true;m.forEach((s,i)=>{try{new(require('vm').Script)(s[1]);}catch(e){ok=false;console.log('script#'+i,e.message);}});console.log(ok?'PARSE OK':'PARSE FAIL');"
```
Expected: `PARSE OK`

- [ ] **Step 3: Browser render check (콘솔 에러 없음)**

서버 실행 중이면 `/browse`로 STEP3(계획검수) → "🔍 스타일 분석" 버튼 존재 및 콘솔 에러 없음 확인. (실제 URL 분석은 네트워크 필요 — 렌더/무에러만 확인)

Run:
```bash
B="$HOME/.claude/skills/gstack/browse/dist/browse"
"$B" goto http://localhost:5000/ >/dev/null 2>&1
"$B" console --errors 2>&1 | head
```
Expected: `(no console errors)`

- [ ] **Step 4: Commit**

```bash
git add static/index.html
git commit -m "feat(style): 스타일 분석 결과에 추출 톤·본문부족 경고 표시(doStyle)"
```

---

## 최종 검증 (전체)

- [ ] `venv/Scripts/python.exe -m pytest tests/test_quality.py -q` → 38 passed
- [ ] `venv/Scripts/python.exe -c "import app; print('ok')"` → ok
- [ ] 인라인 스크립트 PARSE OK
- [ ] (선택, 키 있을 때) 실제 참고 URL 1개로 `/api/style` E2E: `extracted_by=="llm"`, 프로파일에 어미/습관 채워짐, 이후 생성 원고에 문체 반영 + 원문 문장 미복붙 확인

---

## Self-Review 결과

- **Spec 커버리지:** §4 컴포넌트(모두 Task 1~4), §5 데이터모델·프롬프트조각(Task 1,3), §7 에러처리(폴백=Task 2, warning=Task 4,5, JSON실패=Task 3, 불변식=Task 1), §8 테스트 5개(→6개로 확장, LLM 라운드트립은 정규화 순수테스트+라우팅 monkeypatch로 대체), §9 변경범위(Task 1~6) 모두 대응. 편차(§5 style_profile 키)는 헤더에 명시.
- **Placeholder:** 없음. 모든 코드 스텝에 실제 코드 포함.
- **타입 일관성:** 프로파일 키(`tone`/`ending_patterns`/`examples`/`extracted_by`/`warning`/`source_urls`)가 Task 1~5에서 동일하게 사용됨. `extract_profile`/`_extract_profile_llm`/`_normalize_profile`/`_profile_from_regex`/`_scrape` 시그니처 일관.
