# 가안→피드백→최종본 재작성 (F2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 편집 화면에서 자연어 피드백 한 번으로 가안을 최종본으로 재작성한다 — 사진·위키박스·경로카드는 코드가 잠가 AI가 만질 수 없게.

**Architecture:** `_protect_assets`가 코드 조립물을 `[KEEP_n]` 토큰으로 치환(사진 base64 1.7MB → 텍스트 수 KB), LLM revise 후 `_restore_assets`가 무결성 검증하며 복원(결손 시 revise 거부, 원본 무손상). `/api/revise`는 기존 백그라운드+폴링 관례. UI는 renderS4 카드 1개.

**Tech Stack:** Python 3, Flask, pytest(monkeypatch), 인라인 JS(`startPoll(cb)` 관례 — done/error 시 cb 호출·자동 정지).

**기준 커밋 `76e35ee`.** 라인이 어긋나면 함수명/문자열로 찾는다.

---

## File Structure

- `core.py` — `_find_asset_spans`/`_protect_assets`/`_restore_assets`(Task 1), `revise_draft` + `generate_drafts`에 `post["region"]` 저장(Task 2)
- `app.py` — `/api/revise` 라우트(Task 3)
- `static/index.html` — renderS4 피드백 카드 + `doRevise()`(Task 4)
- `tests/test_quality.py` — 3개 (47 → 50 passed)

---

### Task 1: `_protect_assets` / `_restore_assets` — 자산 잠금 (순수)

**Files:**
- Modify: `core.py` — `TravelBlogGenerator`에 staticmethod 3개 추가(`_number_places` 근처)
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing tests** (파일 끝에 추가)

```python
_ASSET_HTML = (
    '<p>인트로 문장.</p>'
    '<div style="background:#f7f8f3;border:1px solid #e3e7d8">위키박스 내용</div>'
    '<p>본문 A</p>'
    '<figure style="text-align:center"><img src="data:image/jpeg;base64,AAAA"/></figure>'
    '<p>본문 B</p>'
    '<div style="background:linear-gradient(135deg,#eef6ff,#f0f4ff)">'
    '<div style="font-weight:bold">🚶 이동 A → B</div>'
    '<div style="color:#6b7280">10분</div></div>'
    '<p>마무리</p>'
)


def test_protect_restore_roundtrip():
    text, assets = TravelBlogGenerator._protect_assets(_ASSET_HTML)
    assert len(assets) == 3                      # 위키박스 + figure + 경로카드
    assert "base64" not in text                  # 무거운 자산이 텍스트에서 제거됨
    assert "[KEEP_1]" in text and "[KEEP_3]" in text
    assert "본문 A" in text and "본문 B" in text  # 본문은 남음
    restored = TravelBlogGenerator._restore_assets(text, assets)
    assert restored == _ASSET_HTML               # 무손실 왕복


def test_restore_rejects_missing_token():
    text, assets = TravelBlogGenerator._protect_assets(_ASSET_HTML)
    # 토큰 삭제 → 거부
    assert TravelBlogGenerator._restore_assets(text.replace("[KEEP_2]", ""), assets) is None
    # 토큰 중복 → 거부
    assert TravelBlogGenerator._restore_assets(text + "[KEEP_1]", assets) is None
    # None 입력 안전
    assert TravelBlogGenerator._restore_assets(None, assets) is None
```

- [ ] **Step 2: Run to confirm FAIL**

Run: `venv/Scripts/python.exe -m pytest tests/test_quality.py::test_protect_restore_roundtrip tests/test_quality.py::test_restore_rejects_missing_token -q`
Expected: FAIL (AttributeError)

- [ ] **Step 3: Implement** — `TravelBlogGenerator`에 추가:

```python
    @staticmethod
    def _find_asset_spans(content):
        """코드 조립물(사진 figure·위키박스·경로카드) 구간 [(start,end)]을 찾는다.
        div 블록은 중첩이 있어 깊이 카운트로 닫는 지점을 찾는다. 순수함수."""
        spans = []
        for m in re.finditer(r'<figure[^>]*>.*?</figure>', content, re.DOTALL | re.IGNORECASE):
            spans.append((m.start(), m.end()))
        opener = re.compile(
            r'<div[^>]*style="[^"]*(?:background:#f7f8f3|linear-gradient)[^"]*"[^>]*>',
            re.IGNORECASE)
        for m in opener.finditer(content):
            depth = 0
            for t in re.finditer(r'<div\b|</div>', content[m.start():], re.IGNORECASE):
                depth += 1 if t.group().lower().startswith('<div') else -1
                if depth == 0:
                    spans.append((m.start(), m.start() + t.end()))
                    break
        spans.sort()
        merged = []
        for s, e in spans:
            if merged and s < merged[-1][1]:
                merged[-1] = (merged[-1][0], max(merged[-1][1], e))
            else:
                merged.append((s, e))
        return merged

    @staticmethod
    def _protect_assets(content):
        """코드 조립물을 [KEEP_n] 토큰으로 치환 → (경량 텍스트, 자산 리스트).
        AI에게는 텍스트만 주므로 사진/박스/카드를 물리적으로 만질 수 없다."""
        content = content or ""
        spans = TravelBlogGenerator._find_asset_spans(content)
        assets, out, prev = [], [], 0
        for s, e in spans:
            out.append(content[prev:s])
            out.append(f"[KEEP_{len(assets) + 1}]")
            assets.append(content[s:e])
            prev = e
        out.append(content[prev:])
        return "".join(out), assets

    @staticmethod
    def _restore_assets(text, assets):
        """[KEEP_n] 토큰을 원 자산으로 복원. 각 토큰이 정확히 1회씩 없거나
        미지 토큰이 남으면 None(= revise 거부, 원본 유지)."""
        if text is None:
            return None
        for i in range(len(assets), 0, -1):
            tok = f"[KEEP_{i}]"
            if text.count(tok) != 1:
                return None
            text = text.replace(tok, assets[i - 1])
        if re.search(r'\[KEEP_\d+\]', text):
            return None
        return text
```

- [ ] **Step 4: Run full suite** → 49 passed

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(revise): 자산 잠금(_protect_assets/_restore_assets) — 사진·박스·카드를 KEEP 토큰으로 보호"
```

---

### Task 2: `revise_draft` — 피드백 재작성 (LLM)

**Files:**
- Modify: `core.py` — `TravelBlogGenerator.revise_draft` 신규(Task 1 메서드들 뒤) + `generate_drafts`에 `post["region"]` 저장 1줄
- Test: `tests/test_quality.py`

- [ ] **Step 1: Write the failing test**

```python
def test_revise_draft_llm_wiring(monkeypatch):
    import types
    g = TravelBlogGenerator(Config())
    captured = {}
    def fake_create(**k):
        captured["prompt"] = k["messages"][0]["content"]
        # AI가 본문만 고치고 KEEP 토큰은 유지한 응답을 모사
        text, _ = TravelBlogGenerator._protect_assets(_ASSET_HTML)
        return types.SimpleNamespace(choices=[types.SimpleNamespace(
            message=types.SimpleNamespace(content=text.replace("본문 A", "더 감성적인 본문 A")))])
    monkeypatch.setattr(g.client.chat.completions, "create", fake_create)
    post = {"content": _ASSET_HTML, "tags": ["기타큐슈"], "title": "제목"}
    out = g.revise_draft(post, "더 감성적으로", None)
    assert out is not None
    assert "더 감성적인 본문 A" in out["content"]
    assert "base64,AAAA" in out["content"]            # 자산 복원됨
    assert "피드백" in captured["prompt"] and "더 감성적으로" in captured["prompt"]
    assert "KEEP" in captured["prompt"]               # 토큰 유지 규칙 포함
    # 빈 피드백 → None
    assert g.revise_draft(post, "  ", None) is None
    # AI가 토큰을 삭제한 응답 → None (원본 무손상)
    def bad_create(**k):
        return types.SimpleNamespace(choices=[types.SimpleNamespace(
            message=types.SimpleNamespace(content="토큰 다 날린 응답"))])
    monkeypatch.setattr(g.client.chat.completions, "create", bad_create)
    assert g.revise_draft(post, "피드백", None) is None
```

- [ ] **Step 2: Run to confirm FAIL** → AttributeError

- [ ] **Step 3: Implement**

```python
    def revise_draft(self, post, feedback, naver_analysis=None):
        """가안 + 자연어 피드백 → 최종본 post 재작성. 실패 시 None(원본 무손상).
        코드 조립물은 [KEEP_n]으로 잠가 AI가 만질 수 없다(형식은 코드 불변식)."""
        feedback = (feedback or "").strip()
        content = (post or {}).get("content") or ""
        if not feedback or not content:
            return None
        text, assets = self._protect_assets(content)
        prompt = (
            "아래는 여행 블로그 글(HTML 조각)이다. 사용자 피드백을 반영해 수정하라.\n"
            "[규칙]\n"
            "1. 피드백에 해당하는 부분만 고치고, 나머지 문장·HTML 태그·구조는 그대로 유지한다.\n"
            "2. [KEEP_숫자] 토큰은 절대 추가/삭제/수정하지 않는다 — 원래 자리에 그대로 둔다.\n"
            "3. 구글맵/이동경로/URL/이미지 태그를 새로 넣지 않는다 (코드가 자동 삽입).\n"
            "4. 수정된 글 전체를 그대로 출력한다. 설명·코드펜스 금지.\n\n"
            f"[사용자 피드백]\n{feedback}\n\n[글]\n{text}"
        )
        try:
            r = self.client.chat.completions.create(
                model=self.model,
                messages=[{"role": "user", "content": prompt}],
                temperature=0.4, max_tokens=self.max_tok,
            )
            out = (r.choices[0].message.content or "").strip()
        except Exception as e:
            logger.warning(f"⚠️ 피드백 재작성 LLM 실패: {e}")
            return None
        if out.startswith("```"):
            out = re.sub(r'^```[a-zA-Z]*\n?', '', out)
            out = re.sub(r'\n?```$', '', out).strip()
        restored = self._restore_assets(out, assets)
        if not restored:
            logger.warning("⚠️ 피드백 재작성 거부: KEEP 토큰 결손/중복 — 원본 유지")
            return None
        if out.count("http") > text.count("http"):
            logger.warning("⚠️ 피드백 재작성 거부: 신규 URL 유입 — 원본 유지")
            return None
        new_post = dict(post)
        new_post["content"] = self._number_places(restored)
        new_post["seo_warnings"] = self.seo_check(
            new_post, naver_analysis, (post or {}).get("region", ""))
        return new_post
```

그리고 `generate_drafts`의 post 완성 블록(`post["group_label"] = group_label` 다음 줄)에 revise의 지역 검증용으로 1줄 추가:

```python
                post["region"] = region
```

- [ ] **Step 4: Run full suite** → 50 passed. Import: `venv/Scripts/python.exe -c "import core; print('ok')"` → ok

- [ ] **Step 5: Commit**

```bash
git add core.py tests/test_quality.py
git commit -m "feat(revise): revise_draft — 피드백 반영 최종본 재작성(자산 잠금·실패 시 원본 무손상)"
```

---

### Task 3: `/api/revise` 라우트

**Files:**
- Modify: `app.py` — `/api/generate_all` 라우트 뒤에 추가

- [ ] **Step 1: Add route**

```python
# ── 피드백 재작성 (가안 → 최종본) ──
@app.route('/api/revise', methods=['POST'])
def api_revise():
    data = request.json or {}
    gi = int(data.get("gi", 0))
    di = int(data.get("di", 0))
    feedback = (data.get("feedback") or "").strip()
    if not feedback:
        return jsonify({"error": "피드백이 비어있습니다"}), 400
    gs = state["group_states"].get(gi, {})
    drafts = gs.get("drafts", [])
    if di >= len(drafts):
        return jsonify({"error": "초안 없음"}), 400
    def task():
        state["progress"] = {"status": "revising", "message": "피드백 반영해 재작성 중...", "percent": 0}
        try:
            new_post = state["generator"].revise_draft(
                drafts[di], feedback, state.get("naver_analysis"))
            if new_post:
                drafts[di] = new_post
                state["progress"] = {"status": "done", "message": "✅ 최종본 재작성 완료", "percent": 100}
            else:
                state["progress"] = {"status": "error",
                                     "message": "❌ 재작성 실패 — 원본은 유지됩니다", "percent": 0}
        except Exception as e:
            state["progress"] = {"status": "error", "message": f"❌ {e} (원본 유지)", "percent": 0}
    threading.Thread(target=task, daemon=True).start()
    return jsonify({"started": True})
```

- [ ] **Step 2: Verify** — `venv/Scripts/python.exe -c "import app; print('ok')"` → ok, 그리고 빈 피드백 400 스모크:

```bash
venv/Scripts/python.exe -c "
import app; app.init_engine()
c = app.app.test_client()
r = c.post('/api/revise', json={'gi':0,'di':0,'feedback':''})
print(r.status_code)  # 400
r2 = c.post('/api/revise', json={'gi':0,'di':0,'feedback':'x'})
print(r2.status_code)  # 400 (초안 없음)
"
```
Expected: `400` `400`

- [ ] **Step 3: Commit**

```bash
git add app.py
git commit -m "feat(revise): /api/revise 라우트 — 백그라운드 재작성, 실패 시 원본 유지"
```

---

### Task 4: UI — 피드백 카드 + doRevise

**Files:**
- Modify: `static/index.html` — `renderS4`의 SEO 경고 카드 삽입 줄 다음 + `doRevise` 함수 신규

- [ ] **Step 1: Insert feedback card.** `renderS4` 안의 SEO 경고 줄:

```js
const sw=(drafts[curDraft]||{}).seo_warnings||[];if(sw.length){h+=`<div class="card" style="background:#FDF3E7;border-color:#EED9B8"><div class="card-label">⚠️ SEO 점검</div>${sw.map(x=>`<div class="text-xs" style="color:#8A6D1F;margin:2px 0">· ${esc(x)}</div>`).join('')}</div>`}
```

바로 다음 줄에 추가:

```js
if(drafts.length){h+=`<div class="card"><div class="card-label">✍️ 피드백으로 다듬기</div><div class="text-xs text-fg3 mb-6">가안에 대한 요청을 적으면 AI가 최종본으로 재작성합니다. 사진·지역정보·경로는 그대로 보존됩니다.</div><textarea id="fbText" style="min-height:52px" placeholder="예: 더 감성적으로 / 2일차 설명 늘려줘 / 마무리를 따뜻하게"></textarea><button class="btn btn-pri btn-sm mt-6" onclick="doRevise()">✨ 최종본 재작성</button></div>`}
```

- [ ] **Step 2: Add doRevise.** `renderS4` 함수 정의가 끝난 뒤(다음 함수 정의 앞)에 추가:

```js
async function doRevise(){const t=(document.getElementById('fbText')?.value||'').trim();if(!t){alert('피드백을 입력해주세요');return}
if(!confirm('재작성하면 이 초안의 블록 수동 편집 내용이 초기화됩니다. 계속할까요?'))return;
stat('피드백 반영해 재작성 중...',true);
const r=await fetch(`${API}/api/revise`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({gi:curGroup,di:curDraft,feedback:t})});
const d=await r.json();if(d.error){alert(d.error);stat('');return}
startPoll(async p=>{if(p.status==='done'){const rr=await fetch(`${API}/api/drafts/${curGroup}`);drafts=await rr.json();await loadBlocks(curDraft);renderS4();updatePreview();stat('✅ 최종본 재작성 완료')}else{alert(p.message||'재작성 실패 — 원본은 유지됩니다')}})}
```

(`startPoll(cb)`는 done/error 시 `cb(p)` 호출 후 자동 정지 — index.html:145 기존 관례.)

- [ ] **Step 3: Validate inline script parses**

```bash
node -e "const fs=require('fs');const html=fs.readFileSync('static/index.html','utf8');const m=[...html.matchAll(/<script>([\s\S]*?)<\/script>/g)];let ok=true;m.forEach((s,i)=>{try{new(require('vm').Script)(s[1]);}catch(e){ok=false;console.log('script#'+i,e.message);}});console.log(ok?'PARSE OK':'PARSE FAIL');"
```
Expected: `PARSE OK`

- [ ] **Step 4: Browser render check** — 서버 재시작 후:

```bash
B="$HOME/.claude/skills/gstack/browse/dist/browse"
"$B" goto http://localhost:5000/ >/dev/null 2>&1
"$B" console --errors
```
Expected: `(no console errors)`

- [ ] **Step 5: Commit**

```bash
git add static/index.html
git commit -m "feat(revise): 편집 화면에 피드백으로 다듬기 카드(doRevise) — 가안→최종본"
```

---

## 최종 검증

- [ ] `venv/Scripts/python.exe -m pytest tests/test_quality.py -q` → 50 passed
- [ ] 서버 재시작 → 실초안에 실제 피드백 1회 revise E2E: content 변경 + 사진/위키박스 보존 + `##`/URL 미유입 + UI 갱신 확인

## Self-Review 결과

- **Spec 커버리지:** §2 메커니즘=Task 1·2, §3 컴포넌트 4개=Task 1~4, §4 에러처리(원본 무손상·빈 피드백 400·URL 방어)=Task 2·3 코드에 포함, §5 테스트 3개 전부 대응.
- **Placeholder:** 없음.
- **타입 일관성:** `_protect_assets(content)->(str,list)` / `_restore_assets(text,assets)->str|None` / `revise_draft(post,feedback,naver_analysis)->dict|None` 이 Task 1~3 정의·호출 일치. `post["region"]`은 Task 2에서 저장하고 같은 Task의 seo_check 호출이 소비. 테스트 픽스처 `_ASSET_HTML`은 Task 1에서 정의, Task 2 테스트가 재사용(같은 파일 선행 정의).
