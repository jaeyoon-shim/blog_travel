# 계획 검수 단계 — 개발 진행내역 · 문제사항 · 피드백 기록

작성일: 2026-07-01
대상: `D:\project\blog_writer2` (TravelBlog Pro 웹 에디션)
관련 문서: 설계 `docs/superpowers/specs/2026-06-30-계획-검수-단계-design.md`, 구현계획 `docs/superpowers/plans/2026-06-30-계획-검수-단계.md`, 교훈 `.claude/rules/lessons-learned.md`

> 이 문서는 "계획 검수 단계" 기능의 **실행 기록**이다. 무엇을 만들었는지(단계별), 어떤 순서로 진행했는지, 어떤 문제를 만났고 어떻게 고쳤는지, 사용자 피드백을 어떻게 반영했는지, 무엇을 검증했고 무엇이 남았는지를 정리한다.

---

## 1. 개요

사진 분석과 AI 원고 생성 **사이에 "계획 검수" 단계**를 추가했다. 프로그램이 `Day → 장소 → 사진` 초안을 자동 생성하면, 사용자가 장소명·일정·사진·사건/느낌·별점·영수증(가격)을 확인·수정한다. 확정된 계획(`plan.json`)이 **생성의 단일 진실원**이 되며, 프로젝트 불변식 **"형식은 코드가 조립, AI는 본문만"** 을 지킨다.

**핵심 원칙**: 지도·별점·가격 등 형식은 코드가 조립하고, 사건/느낌/지시 등 재료만 AI에 전달한다. **발행 HTML에는 어떤 URL/지도도 넣지 않는다**(네이버 SE3가 링크카드로 변환해 망가짐). 지도는 검수 화면(내부 도구)에서만 보여준다.

---

## 2. 최종 5단계 워크플로우 (단계별)

```
1. 업로드 → 2. 계획 검수 → 3. AI 생성 → 4. 편집 → 5. 발행
```

### STEP 1 · 업로드
- **📷 여행 사진** 드롭존 + **🧾 영수증** 드롭존(분리). 영수증은 올리는 즉시 OCR(가게명·금액·날짜) → `state["receipts"]`.
- "분석 시작" → EXIF GPS·Vision 분석.

### STEP 2 · 계획 검수 (핵심)
- 상단: **무료 모드 배너**(상호명 정확도 낮음, 검수 권장), 접이식 "여행 설정(제목·참고 URL)".
- **Day별 카드**: 각 장소(stop)마다
  - 장소 이름(자동 확신시 채움, 무료모드면 빈칸)
  - **사진 썸네일(112px, 클릭 확대)** + 💬 장면설명 힌트
  - **인라인 지도**(OSM 핀) + "📍 구글 지도에서 열기"
  - 📝 무슨 일이 있었나 / 💭 느낀 점 / ✍️ AI 지시 / ⭐ 별점
  - **🧾 영수증**: 업로드 단계 영수증이 가게명↔장소명(+날짜)으로 자동 매칭돼 표시, "장소 변경" 드롭다운으로 재배정. 상단 "미배정 영수증" 칸에서 수동 배정.
  - **수동 묶기**: "🔗 다른 장소와 합치기" 드롭다운, 썸네일 "✂ 분리"(새 장소로).
- **자동 저장**(입력시 디바운스) + **💾 수동 저장** 버튼.
- 오른쪽: **게시물 구조 개요**(Day별로 제목→인트로→장소(사진·요약)→이동→팁→마무리).

### STEP 3 · AI 생성
- 글 스타일 선택.
- **📅 날짜별 일정 목록** — Day별 장소·사진수 표시, 클릭해 선택.
- "✨ 선택한 Day만 생성" / "🚀 전체 일정 생성". 실패 시 원인 팝업.
- 오른쪽: 게시물 구조 개요.

### STEP 4 · 편집
- Day 탭 전환, 블록(텍스트/이미지/구분선/라벨/경로) 편집, 초안 선택.
- 오른쪽: **실제 완성 글 미리보기**(위키박스·경로·별점/가격 줄 포함).

### STEP 5 · 발행
- 제목·태그·발행방식·공개설정.
- "📤 발행"(현재 Day) / "🚀 전체 일정 한번에 발행"(확인창 후 Day별 각각).

---

## 3. 진행 내역 (구현 순서)

### 3.1 계획 검수 코어 (백엔드, 순수함수 TDD)
- `core.py` `TripPlanner`: `_photo_id`(안정 ID), `_assign_day`(새벽4시 컷오프), `build_draft`(초안), `_stops_for_day`(장소 묶기 — 확신명/GPS클러스터/개별), `groups_from_plan`(확정 plan→생성기 그룹), `match_receipts_to_stops`(영수증 자동매칭).
- `core.py` `ReceiptReader`: `_parse_amount`(금액·통화), `crosscheck_name`(가게명↔장소명), `read`(Vision OCR).
- `core.py` `TravelBlogGenerator`: `_format_meta_line`(별점·가격 코드조립, URL 없음), `_insert_meta_lines`(장소 h2 뒤 삽입).
- `app.py`: `plan.json` 영속화, `/api/plan*`(draft/get/post/이동/제외/영수증), `/api/receipts/upload`, `/api/photos`에 `pid` 추가, `_active_groups`(plan→Day그룹). 생성 라우트가 plan 사용.

### 3.2 검수 UI (프론트, `static/index.html`)
- 2단계를 "계획 검수"로 교체(렌더·편집저장·영수증·매칭·썸네일·지도·묶기).
- 3단계 Day 목록, 5단계 전체 발행 버튼, 오른쪽 구조 개요.

### 3.3 UX/시각
- 테마: 부드러운 상아색 + 연두(`:root` 팔레트).
- 카드 시각 정리(썸네일 크게·필드 라벨/예시·여백).

### 3.4 자가 QA & 버그 수정
- 실제 OpenAI로 생성 E2E, 각 단계 브라우저 검증, 발견 버그 수정.

전체 유닛테스트 `tests/test_quality.py` **27 passed** 유지.

---

## 4. 문제사항 & 해결 (중요)

| # | 문제 | 원인 | 해결 |
|---|---|---|---|
| P1 | 검수 UI가 실제 앱에 안 보임 | `app.py`가 **`static/index.html`** 서빙인데 UI 작업을 **루트 `index.html`**(미서빙)에 함 | static/index.html로 포팅. CLAUDE.md의 "index.html" 표기가 모호 |
| P2 | 웹 UI 전체가 안 돎(정적 스켈레톤만) | `static/index.html` 인라인 스크립트에 **짝 없는 `}`**(updateSettingsPreview) → SPA 통째로 파싱 실패 (**기존 버그**) | `}` 제거. `node vm.Script`(=V8)로 파싱 검증 도입 |
| P3 | 검수 카드가 빈칸만·사진 안 보임 | 카드가 "사진 N장" 텍스트만, photo_id 매칭 불가 | `/api/photos`에 `pid` + 썸네일·💬장면힌트 표시 |
| P4 | 미배정 영수증 글자 세로 깨짐 | flex 행에서 긴 옵션 `<select>`가 최소너비 키워 `<span>` 0폭 | 세로 스택 + select `max-width` |
| P5 | 영수증 첨부 위치 못 찾음 | 카드마다 밋밋한 file input | 업로드 단계로 이동 + 자동 매칭(사용자 선택) |
| P6 | **편집(4단계) 화면 전체 크래시** | `renderS4`가 프론트에 없는 전역 `state` 참조(`state?.group_states`) → ReferenceError → 렌더 중단 (**생성 후 편집 불가, 치명적**) | `i<=curGroup`로 대체 + 초안 자동 로드 |
| P7 | "초안 생성 안 됨" | 실제 생성 실패해도 `startPoll` 콜백이 **done만 처리** → 조용히 사라짐 | error·빈 초안 시 원인 팝업. (OpenAI 키·모델은 정상 확인) |
| P8 | 프로필 불러오기 멈춤 | `/api/profiles/<name>` 함수 인자 `n`↔`name` 불일치 → **500** (**기존 버그**) | `n`→`name`. `⚙️설정`이 프로필선택(-1) 아닌 **설정화면(0)** 열도록 |
| P9 | 3단계에 날짜별 분리 없음 | Day 선택 UI 부재("현재 선택"만) | 3단계에 **Day별 목록(선택)** 추가 |
| P10 | 시드 서버 혼동 | 여러 포트에 서버 남으면 SO_REUSEADDR로 옛 서버 응답; Chrome이 5060/5061(SIP) 차단 | 재검증 전 `taskkill //F //IM python.exe`, 8088+ 안전 포트 |

**공통 교훈**: 인라인 `<script>`는 구문 OK여도 **런타임 미선언 참조/에러 무처리로 "조용히" 죽는다.** 유닛/코드리뷰로 부족 → `/browse`로 각 단계 실제 렌더 + **실제 생성 E2E**까지 확인. (자세히 `.claude/rules/lessons-learned.md` 항목 5~9)

---

## 5. 사용자 피드백 & 반영

| 피드백 | 반영 |
|---|---|
| "일별 코스 이미지·사진 편집·문체·일정 분리·사전 검수 단계" | 계획 검수 단계로 설계·구현(이미지/보정/문체는 별도 하위프로젝트로 분리) |
| "장소마다 이름뿐 아니라 사건·느낌 등 다양하게 입력" | 장소 카드에 사건/느낌/AI지시/별점 필드 |
| "영수증 올리면 가격·별점 기록" | 영수증 OCR + 별점 + 블로그 표시(토글) |
| "네이버 상위 10개 구조로 생성" | 기존 `NaverBlogAnalyzer` 확장은 **백로그 E**(스크래핑 리스크 분리) |
| "UX/UI 더 편하게" | 카드 시각 정리(라벨·예시·여백), 썸네일 크게 |
| "상아색 바탕 + 연두 베이스" | `:root` 팔레트 교체(다크→라이트) |
| "사진·어떤 장소인지 안 보여주고 입력만 시킴" | 썸네일 + 장면설명 힌트 |
| "영수증 어디에 첨부?" | 업로드 단계 + 자동 매칭 |
| "중간중간 저장 + 수동 저장 버튼" | 자동저장(디바운스) + 💾 버튼 |
| "같은 위치면 묶기(자동+수동)" | GPS 자동 묶기 + 🔗합치기/✂분리 |
| "구글 지도로 위치 표시" | 인라인 OSM 지도 + 구글맵 링크(검수화면만) |
| "1day씩 나누고 전체 한번에 발행" | Day별 생성·발행 + 전체 버튼 |
| "초안 생성 안 됨" | 에러 표시 + 진단(키 정상) + 편집 크래시 수정 |
| "프로필 안 불러와짐" | 500 수정 + 설정화면 연결 |
| "오른쪽 미리보기 무슨 의미? 단계별로" | 2·3단계=구조 개요, 4·5단계=실제 글 |
| "전체 확인·자가테스트·기록" | 자가 QA + 이 문서 |

---

## 6. 검증 내역

- **유닛테스트**: `tests/test_quality.py` 27 passed (순수함수: photo_id·day배정·build_draft·groups_from_plan·매칭·금액파싱·교차검증·meta라인·outline 등).
- **API 통합**(헤드리스 test_client, 네트워크 X): draft·편집저장·이동·제외·영수증 라우트·groups 정합.
- **실제 생성 E2E**(`init_engine` + 진짜 OpenAI): 초안 1개(본문 10KB), 사진 삽입, **`<p>¥2,000</p>` 코드조립**(오타루 운하 h2 뒤), **구글맵 URL 미누출**, 편집 23블록·미리보기 정상.
- **브라우저(`/browse`) 렌더 검증**: 각 단계 UI, 썸네일·지도·매칭·합치기/분리·자동저장·Day목록·구조개요·설정 로드값.
- **미검증(사용자 몫)**: **실제 네이버 발행**(로그인 필요). 발행 버튼·확인창·`/api/publish`·`/api/publish_all` 배선은 완료.

---

## 7. 참조 (데이터·API·파일)

- **plan.json**: `plans/<여행이름>/plan.json` (gitignore). 구조는 설계문서 §3.
- **주요 API**: `/api/plan/draft·plan(GET/POST)·plan/stop/move·plan/exclude·plan/receipt`, `/api/receipts/upload`, `/api/photos`(pid·thumbnail), `/api/groups`(=Day그룹), `/api/generate·generate_all`, `/api/publish·publish_all`.
- **핵심 파일**: `core.py`(TripPlanner·ReceiptReader·TravelBlogGenerator), `app.py`(라우트·`_active_groups`), `static/index.html`(**서빙되는 SPA**).
- **gitignore 추가**: `plans/`, `profiles/`, `last_profile.txt`, `.gstack/`.

---

## 8. 남은 일 / 백로그

- **실제 네이버 발행 검증**(로그인 세션 필요) — Day별/전체 발행 실측.
- **하위프로젝트**(설계 시 분해): B 문체 따라하기, C 일별 코스 이미지, D 사진 보정, **E 네이버 상위구조 기반 생성**(연결점=확정 region 은 `/api/seo`에 심어둠).
- **무료 모드 정확도 개선**: 장소명 비었을 때 GPS 동네명이라도 자동 채우기(선택).
- **로그 인코딩**(Windows cp949): 로깅 핸들러 UTF-8 + `errors="replace"`(백로그 #6).
- **리팩터**: `core.py` God-class, "장소 이름 해석" 6개 메서드 분산(AI 작업 전 `/refactor-audit`).

---

## 9. 주의사항 (불변식·지뢰)

1. **서빙 파일은 `static/index.html`** (루트 `index.html` 아님). UI 작업은 여기서.
2. **발행 HTML에 URL/지도 금지** — 지도는 검수 UI에만. 별점·가격도 코드가 조립(URL 없음).
3. **인라인 스크립트 검증**: `node -e "new (require('vm').Script)(scriptSrc)"`(V8=브라우저). 프론트/백엔드 전역명 겹침(예: `state`) 주의 — 조용히 죽는다.
4. **되돌리기 어려운 외부작업**(전체 발행 등)은 `confirm()` + 안전 기본값(공개설정=비공개).
5. **cp949 콘솔**: `logger`/`print`에 이모지 금지(HTML 출력 문자열은 무방).
6. **재검증 전 서버 정리**: `taskkill //F //IM python.exe`, 안전 포트(8088+).
