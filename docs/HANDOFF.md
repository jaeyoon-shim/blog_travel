# HANDOFF — TravelBlog Pro

다음 세션이 바로 이어받기 위한 서술형 요약. (마커 `<!-- AUTO-SNAPSHOT -->` 아래 블록은 SessionEnd 훅이 자동 생성 — 건드리지 말 것)

## 무엇을 했나 (2026-08-17)

6주 공백 후 재개. 핸드오프의 남은 후보 중 **측정으로 우선순위를 다시 정하고** 두 건을 근본수정했다.

1. **발행 진행표시 배선** (`a9c5948`)
   - 발행 task가 "발행 준비 중"을 1회만 세팅해 사진 삽입 루프(34장 ≈ 12분) 동안 멈춘 듯 보이던 문제.
   - `posters.publish_progress()` 순수함수 + `NaverSeleniumPoster.post(progress_cb=)` 단계별 보고(준비/에디터/제목/**블록별+사진 카운터**/발행창/공개설정/태그/최종확인) → `NaverPoster` 파사드 → `/api/publish`·`/api/publish_all`이 `state["progress"]`에 반영. 전체 발행은 글 내부 진행률을 `[i, i+1]/total` 구간에 매핑.
   - 프론트는 이미 `/api/status`를 800ms 폴링하므로 UI 변경 없음.

2. **계획검수 장소 과분할 근본수정** (`b2af99b`, 후속 `85b9f9b`)
   - `TripPlanner._stops_for_day`를 '확신 이름 키 + ~110m 격자' → **시간·거리 세션 클러스터링(45분·300m)** 으로 교체. 무료 모드 역지오코딩 세부명(관광지/공원/거리/신사/주차장)이 같은 장소 안에서 갈리며 stop이 심하게 쪼개지던 문제.
   - `groups_from_plan`: 사이에 다른 장소가 낀 동명 stop = **재방문**으로 구분(`이름 (재방문)`). 이전엔 하류가 전부 name 키로 묶어 두 방문이 한 섹션으로 재병합되고 메모·별점·지시가 서로를 덮어썼다. **연속된 동명 stop은 재방문이 아니다**(헛 섹션 방지).
   - `merge_user_edits`: 사용자가 손댄 구 stop을 재초안이 다시 쪼개면 되붙인다(사용자 확정이 진실원).

## 2026-08-17 (2차) — 0원 위치 공유 + 입력 정리 (`0b70757`, `1c4f5c5` 푸시됨)

**무료 설계**: 유료 Places 키 없이도 독자에게 정확한 위치를 공유한다.
- 사진 좌표 → 구글맵 검색 URL(키 불필요)을 **하이퍼링크**로 삽입(`<!--MAPLINK-->` → `map_link` 블록 → `_paste_hyperlink`). OG 카드가 아니라 링크라 레이아웃을 해치지 않고 본문 URL 미노출 불변식도 유지.
- 계획검수 stop마다 **위치 링크 입력 필드**(비우면 좌표 자동, 붙여넣으면 그 링크 우선).
- 위키백과 장소 카드는 이름 정확일치 + 좌표 30km 교차검증일 때만(현 프로젝트 데이터에선 채택 0/20 — 조용히 생략).
- 실측 근거: 키 없는 구글맵 URL은 og:title이 비어 일반 카드, 네이버 지도 URL은 OG 없음, OSM 타일 자동 캡처는 타일 정책 위반 소지.

**입력 정리**: 23개 → 기본 8 + 고급 8. 네이버 ID/PW(미사용인데 평문 저장이었다)·발행방식·Google 키·중복 URL칸 삭제, 길이 2종 → 분량 1개, 말투·톤 → 문체 소스로 통합, 나머지는 고급 접기. `migrate_settings()`가 옛 파일을 자동 변환(멱등)하고 `save_settings()`는 자격증명을 파일에 쓰지 않는다.

테스트 **108 passed**. `/browse` 실렌더 확인(콘솔 에러 0), 실데이터 체인(UI 입력 → plan.json → 생성 → 발행 블록 → 저장 HTML) 통과.


## 현재 상태

- **테스트 108 passed** (`venv/Scripts/python.exe -m pytest tests/test_quality.py -q`). 커밋 6개 모두 `origin/web-edition`에 푸시 완료.
- 클러스터링 정확도 — 사용자가 확정한 plan stop을 정답 레이블로 둔 사진쌍 F1: **0.456 → 0.940** (6개 일차). 자동 stop 총 45개 → 21개(사용자 확정 20개).
- 실 E2E 통과: ① `/api/publish` 진행 스냅샷 14개·단조증가·사진 카운터 노출 ② `/api/publish_all` 그룹 구간 매핑 ③ `/api/plan/draft` 실데이터(디스크 미기록) ④ **실제 OpenAI 생성** — 재방문 있는 1일차에서 `연화의湯` / `연화의湯 (재방문)` 섹션 분리, 사진 12/12 순서 일치, 첫 h2 이전 figure 0 ⑤ 실데이터 재초안 시 사용자 편집 보존.
- 기존 사용자 데이터는 안전: 확정된 `plan.json`은 변경되지 않는다(새 로직은 **초안 생성** 경로에만 적용).

## 다음 단계

1. **위치 링크·진행표시 실발행 검증** — 새 `map_link`(하이퍼링크)와 발행 진행표시는 코드·블록 레벨까지 검증했지만 **실제 네이버 발행은 아직**이다. 다음 발행 때 ① 링크가 OG 카드로 변하지 않고 텍스트 링크로 들어가는지 ② 진행률이 사진 카운터로 흐르는지 확인.
2. ~~지도 카드 유료 키 결정~~ → 무료 경로로 대체 완료. 유료 키를 넣으면 기존 구글맵 place 카드 경로가 그대로 우선한다.
3. **2일차형 과병합 한계** — 45분·300m로는 인접한 두 장소(기타노 이진칸 ↔ 고베 거리 구경)를 가르지 못한다(6일차 중 유일한 과병합). 필요하면 체류 밀도/방향 전환 같은 신호 추가 검토. 지금은 사용자가 ✂분리 1회로 해결 가능.
4. 신규 기능 백로그: G5 Wikimedia 명소 이미지, 로드맵 C(일별 코스 이미지), D(사진 자동 보정).
5. 기술부채: `core.py` God-class 리팩터, 로그 UTF-8 인코딩(lessons #6), 사장 코드 정리.

## 열린 결정 / 주의

- **`place_groups` 비연속 재방문 병합은 실데이터에서 문제가 아니었다** — 핸드오프에 최우선으로 적혀 있었으나 stop 시간구간 교차 0건이었고, 섹션 구조를 정하는 층은 GPS `location_name`이 아니라 **plan stop**이다. 그대로 구현했으면 3일차 섹션이 6개→49개로 폭발했다. 코드 독해로 만든 백로그는 착수 전 실데이터로 재확인할 것.
- 재방문 표시가 `연화의湯 (재방문) (延羽の湯)`처럼 괄호가 겹쳐 보일 수 있다(AI가 현지명을 덧붙임). 검수 화면에서 stop 이름을 다르게 바꾸면 접미는 붙지 않는다.
- 발행 Chrome은 절대 taskkill 금지(네이버 세션 무효화) — 코드가 `poster.close()`로 정리한다. 서버 재시작 후 5000 리스너가 1개인지 확인(`app.py`가 이중 기동은 차단함).

<!-- AUTO-SNAPSHOT -->
