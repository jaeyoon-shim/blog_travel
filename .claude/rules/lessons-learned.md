# Lessons Learned

프로젝트 작업 중 얻은 교훈과 미적용 백로그. (전역 규칙: 에이전트 실수/교훈은 여기 기록)

---

## 2026-06-22 ~ 06-24 — 무료 모드 전환 + 발행 검증

### 교훈

1. **외부 발행의 공개설정은 "안전 기본값"이어야 한다 (안전 버그)**
   - `_set_visibility`의 `vis_map`이 영문 키(`private`/`public`)만 인식했는데, 앱 기본값은 한글 `"비공개"` → 매핑 실패 → 폴백이 **`전체공개`**(공개!)였다.
   - 즉 사용자가 "비공개"를 골라도 **전체공개로 발행**되는 사고 위험.
   - **교훈**: 되돌리기 어려운 외부 작업(발행 등)에서 알 수 없는 입력은 **가장 안전한 값(비공개)** 으로 폴백. 기본값을 절대 "공개"로 두지 말 것. → `_visibility_target()`로 추출 + 한글/영문 인식 + unknown→비공개.

2. **폴백 경로는 주 경로와 "동일한 데이터 계약"을 채워야 한다 (무료 모드의 조용한 품질 저하)**
   - Google Maps → Nominatim 폴백 시 `_nominatim_geocode`가 `city/region/country`를 안 채워(`_google_geocode`는 채움) → 지역 감지 실패 → 위키 박스가 "홋카이도 삿포로" 환각.
   - 또 POI 확정 시 번역 스킵 로직이 "Google Places=한글명" 전제 → Nominatim 원문 일본어(`篠崎八幡神社`)가 제목/본문 누출.
   - **교훈**: 폴백을 추가하면, 폴백이 반환하는 dict가 주 경로와 **같은 필드 계약**을 만족하는지, 그리고 그 값을 소비하는 **모든 다운스트림**을 점검할 것.

3. **LLM에 "개수 할당"을 강요하면 환각한다**
   - 위키 박스 프롬프트의 `specialties/foods/spots 각 3~5개`가 환각 주원인(개수 채우려 인접지 명소를 지어냄).
   - **교훈**: `0~N개, 확실한 것만, 없으면 빈 배열` + `temperature=0`. 개수 하한을 두지 말 것.

4. **Windows cp949 콘솔에서 이모지 로그 크래시** (미해결, 백로그 #6)
   - `logger.info("✅ ...")` / `print("🔬 ...")`가 `UnicodeEncodeError`로 스크립트를 죽인다.
   - 임시 회피: 스크립트 진입부 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`.

5. **네트워크/Selenium 로직에서 순수 결정 부분을 staticmethod로 추출 → 유닛테스트**
   - `_pick_admin`(주소→행정구역), `_dedupe_separators`(구분선 정규화), `_visibility_target`(공개설정 매핑) 추출로 네트워크 없이 회귀 테스트 가능해짐. `tests/test_quality.py` 관례 활용.

### 검증된 사실
- 무료 모드(Google 키 비활성화)에서 전체 파이프라인 정상 + 실제 네이버 **비공개 발행 성공**.
- 자동 로그인은 네이버 보안에 막힘 → `selenium_profile` 세션 + 수동 로그인 폴백(180초)으로 통과. 한 번 로그인하면 세션 저장.

### 미적용 백로그
- **#4 무료 모드 가시성**: 무료 모드일 때 로그 배너 + UI에 "상호명 정확도 낮음, 검수 권장" 노출. (지금은 조용히 품질 저하)
- **#6 로그 인코딩**: 로깅 핸들러/스트림에 UTF-8 + `errors="replace"` 적용 (Windows 안정성).
- **리팩터**: "장소 이름 해석" 책임이 6개 메서드(`_nominatim_geocode`/`_google_geocode`/`_make_place_name`/`_translate_to_korean`/`_koreanize_region`/`_detect_region`)에 분산. `core.py` 2,600줄 God-class 2개. AI 작업 전 `/refactor-audit` 권장.
- 상세 리뷰: `docs/reviews/2026-06-22-free-mode-review.md`

---

## 2026-06-30 — 계획 검수 단계 추가

1. **계획 검수(plan review)가 생성의 단일 진실원**: 분석 완료 후 TripPlanner가 초안을 만들고, 사용자가 일정/장소명/사건/느낌/별점/영수증을 검수·확정하면 `plans/<title>/plan.json`이 생성된다. 이후 generate, /api/seo 등 모든 다운스트림은 이 plan을 참조해야 한다(`_active_groups`가 plan 기반 그룹을 봐야 /api/groups·/api/blocks·generate 간 gi 정합이 유지됨).

2. **함정 — 그룹 소스 불일치**: plan 흐름에서 `/api/groups`, `/api/blocks`, `generate`가 모두 같은 그룹 소스(`_active_groups`)를 봐야 gi 인덱스가 맞는다. 한 곳이라도 원래 `state["groups"]`를 직접 참조하면 프론트 목록과 생성 그룹이 어긋난다.

3. **영수증 이미지 발행 제외**: 영수증 사진은 개인정보이므로 발행 블록에서 제외. 파일명은 `werkzeug.utils.secure_filename`으로 정제. 별점·가격은 코드가 HTML 조립(`_format_meta_line`), URL은 포함하지 않는다.

4. **🔴 서빙되는 UI 파일은 `static/index.html`이다 (루트 `index.html` 아님)**: `app.py`의 `/`는 `send_from_directory('static','index.html')`. 루트 `index.html`은 서빙 안 되는 레거시 중복본. UI 작업은 **반드시 `static/index.html`** 에 한다. (이번에 서브에이전트가 CLAUDE.md의 모호한 "index.html" 표기를 믿고 루트를 고쳐 전부 무효가 됐다 → static/로 포팅해 해결.) CLAUDE.md의 표기도 정정 필요.

5. **🔴 단일 인라인 `<script>`의 구문오류 1개 = SPA 전체 사망**: `static/index.html`은 거대한 단일 인라인 스크립트라, 어디든 구문오류(예: `updateSettingsPreview`의 짝 없는 `}`)가 하나 있으면 **모든 함수·리스너가 미정의**되어 화면이 정적 스켈레톤만 남는다(콘솔에 조용히 죽기도). 발견 당시 이 오류는 **기존부터 존재**(이 작업과 무관)해 웹 UI가 통째로 안 돌고 있었다.
   - **교훈**: UI 변경은 유닛/코드리뷰만으로 부족 — **실제 브라우저 렌더 확인 필수**(`/browse`로 스크린샷). 정적 검증으로 `node -e "new (require('vm').Script)(scriptSrc)"`(V8=브라우저 파서)로 인라인 스크립트 파싱을 검사할 수 있다. (`node --check`는 CJS 래퍼 때문에 오탐 위치를 주지만, vm.Script는 정확.)

6. **영수증 UX = 업로드 단계에서 사진과 함께 + 자동 매칭**: 영수증은 1단계에서 별도 영역(`#rdz`→`/api/receipts/upload`)으로 올려 즉시 OCR, `state["receipts"]`에 보관. 계획 초안 시 `TripPlanner.match_receipts_to_stops`로 **가게명↔장소명 일치(+같은 날짜)** 인 stop에 자동 배정(추측 안 함, 실패분은 `plan["unmatched_receipts"]`). 2단계 장소 카드에서 매칭 표시+재배정, 상단 "미배정 영수증"칸에서 드롭다운으로 배정. 카드별 개별 첨부는 폐기.

7. **`/browse` 테스트 함정 3개**: (a) `browse js`는 **격리 월드**라 페이지 전역(go/render 등) 접근 불가 → DOM은 읽히지만 함수 호출은 안 됨. 네비게이션은 실제 `browse click`으로. (b) Chrome은 **특정 포트(5060/5061=SIP 등)를 ERR_UNSAFE_PORT로 차단** → 로컬 테스트 서버는 8088 등 안전 포트 사용. (c) flex 행에서 `<select>`(긴 옵션)는 최소너비가 커져 옆 `<span>`을 0폭으로 만들어 글자 세로깨짐 → 세로 스택 또는 select `max-width`. (d) 시드 서버를 여러 포트에 남기면 SO_REUSEADDR로 **옛 서버가 응답**해 혼란 → 재검증 전 `taskkill //F //IM python.exe`로 정리.

8. **검수 화면 편의기능 배치(2026-07-01)**: 썸네일은 `/api/photos`에 `pid` 추가해 stop.photo_ids와 매칭(썸네일·힌트·지도). 같은 위치 무명 사진은 `_stops_for_day`에서 **GPS 반올림(~110m) 클러스터**로 묶음. **지도는 검수 UI에만**(OSM iframe + 구글맵 링크, 키 불필요) — **발행 HTML엔 절대 미포함**(불변식). 편집 자동저장은 `#planRoot`에 delegated `input` 리스너 + 디바운스 savePlan. 테마는 `:root` 변수라 팔레트만 바꾸면 전역 반영(상아색+연두). 전체발행은 `/api/publish_all`(그룹별 blocks 수집) — 되돌리기 어려우니 `confirm()` 필수.

9. **자가 QA(2026-07-01) — 프론트 "조용한 실패" 3종**: (a) **미선언 전역 참조**: `renderS4`가 백엔드 전역명 `state`(`state?.group_states`)를 참조 → ReferenceError로 **편집화면 전체가 안 그려짐**(생성 후 편집 불가, 치명적). 프론트/백엔드 전역명 겹침 주의. (b) **폴링 콜백이 error 미처리**: `doGen`/`doGenAll`의 `startPoll`이 `done`만 처리 → 생성 실패해도 조용히 사라져 "생성 안 됨"으로 보임. `status==='error'`·빈 초안 시 alert. (c) **`/api/profiles/<name>`가 함수 인자 `n`**(→`name`) 500 + `goSettings`가 설정(0) 아닌 프로필선택(-1)로 감. **교훈**: 인라인 `<script>`는 구문 OK여도 **런타임 미선언 참조/에러 무처리로 조용히 죽음** → `/browse`로 각 스텝 실제 렌더 + 실제 생성(E2E)까지 확인. 생성 파이프라인은 `init_engine()` 후 실제 OpenAI로 검증(초안·사진삽입·`<p>¥2,000</p>` 코드조립·URL 미누출 확인).

---

## 2026-07-02 — B(문체 따라하기) 구현 + 매니지먼트 관찰

1. **`.gitignore`의 앵커 없는 `plans/`가 `docs/superpowers/plans/`를 삼킴**: 루트 런타임 산출물(`plans/<title>/plan.json`)용 규칙 `plans/`가 앵커가 없어 CLAUDE.md가 커밋하라고 지시한 `docs/superpowers/plans/`까지 무시했다(specs는 무사, plans만 막힘). → `/plans/`로 루트 앵커링해 해결. **교훈**: gitignore 규칙은 의도 범위에 맞게 `/` 앵커링. `git check-ignore <path>`로 오탐 확인.

2. **문체 주입 = "규칙+예시", 원문 통째 금지(표절 불변식)**: `_style_context`는 LLM이 뽑은 규칙 JSON + **예시 문장 1~2개 상한**만 프롬프트에 넣는다. 원문 문단 통째 주입은 표절/복붙 위험이라 폐기. 회귀 테스트가 "긴 원문 문단이 조각에 안 실림"을 assert. 무료모드는 정규식 폴백(`extracted_by="regex_fallback"`), 폴백 어미는 여전히 파편기라 최소보장용.

3. **모델 라우팅 첫 적용(subagent-driven)**: 구현 subagent=sonnet, per-task 리뷰·최종 전체리뷰=메인루프(최고모델) 직접. 작고 완전히 테스트된 태스크는 리뷰어 subagent 남발 대신 컨트롤러가 diff 직접 검증(토큰 절감). **subagent는 부모 모델 상속 안 함 → Agent 호출마다 `model` 명시 필수.**

4. **subagent 세션 한도 중도 사망 대비**: 구현 subagent가 세션 한도로 커밋 전에 죽을 수 있다(Task5에서 발생 — 파일 편집은 됐으나 미커밋). **교훈**: subagent 보고를 믿지 말고 `git status`/`git show`로 실제 반영·커밋 여부 확인. 작은 배선은 컨트롤러가 이어받아 검증·커밋.

5. **post dict에 필드 추가 시 `/api/drafts` 직렬화도 함께 (E2E 테스트에서 발견)**: `/api/drafts/<gi>`는 **화이트리스트 직렬화**(title/content/tags/meta/hashtags만)라, `generate_drafts`가 post에 `seo_warnings`를 넣어도 API 응답에서 빠져 UI 배지가 안 떴다. 백엔드 단위테스트·회귀 전부 통과했는데 실제 E2E에서만 드러남. **교훈**: post 스키마 확장 시 소비 지점 3곳(generate_drafts→/api/drafts 직렬화→프론트) 전부 점검. 계획 단계에서 "데이터가 지나가는 모든 관문"을 나열할 것.

6. **포트 5000에 옛 서버 3개 중첩(#7d 재발)**: bash에서 `powershell -Command "... \"name='python.exe'\" ..."` 이스케이프가 깨져 Stop-Process가 **조용히 실패** → 11시/14시/16시 서버 3개가 SO_REUSEADDR로 같이 리스닝, 옛 코드가 응답해 "count=10인데 방문 3개, 불용어 미적용"처럼 보였다. **교훈**: 재시작 후 반드시 `netstat -ano | grep :5000 | grep LISTENING | wc -l`이 **1인지 확인**. 죽일 땐 PID를 netstat에서 직접 뽑아 `taskkill //F //PID`.

7. **발행 브라우저는 반드시 poster.close()로 닫는다 (강제종료 금지)**: 발행 Chrome을 안 닫으면 selenium_profile 잠금으로 다음 발행 전멸("session not created"), 잠금을 taskkill로 강제 해제하면 **네이버 세션이 무효화**되어 다음 발행에서 수동 로그인(180초)이 다시 필요해진다. 또한 발행이 낳은 **chromedriver가 Flask의 5000 리스닝 소켓을 상속**한 채 살아남아, 서버를 죽여도 유령 리스너가 포트를 잡는다(HTTP 000). → 2026-07-03 수정: 발행 task `finally: poster.close()`(정상 종료라 세션 보존+소켓 해제). 검증: 연속 2건 발행 성공, 2번째는 로그인 생략, chromedriver 0.

8. **curl로 한글 테스트 시 인코딩 2종 함정**: (a) `-F "photos=@한글파일.jpg"` → 멀티파트 파일명 깨져 "파일이 없습니다"(브라우저 업로드는 무관). ASCII 사본으로 테스트. (b) `-d '{"title":"한글"}'` → body가 cp949로 나가 Flask가 400 (utf-8 디코드 실패). UTF-8로 저장한 파일을 `--data-binary @file`로 보낼 것.

---

## 2026-07-03 — I라운드: 참고 포스트 퀄리티 업그레이드

1. **잘 된 남의 포스트는 DOM으로 실측 분석하라**: /browse로 참고 포스트(blog.naver.com/eng1470)의 `se-component` 시퀀스를 추출하니 "구분선→이모지 헤더→사진 콜라주+캡션→본문→구글맵 OG 카드→↑↑위치↑↑" 정형 패턴이 바로 드러났다. 텍스트만 읽어서는 콜라주/oglink 구조를 알 수 없다. 네이버 블로그는 iframe(`mainFrame`)이라 `frame --name mainFrame` 전환 필요.

2. **불변식 완화는 "이유"를 기준으로**: "구글맵 URL 절대 금지"의 이유는 본문 중 URL의 *비의도적* 카드 변환이었다. 참고 포스트는 반대로 독립 블록 URL로 카드를 *의도적으로* 유도한다 — 불변식을 "본문 텍스트 내 URL 금지 + 독립 블록 카드는 코드가 유도"로 정밀화(사용자 승인). AI 프롬프트의 URL 금지는 유지.

3. **에디터 빈 줄에서 Shift+Home은 무선택**: `_paste_map_card` 변환 실패 폴백에서 Enter로 새 빈 줄에 내려온 상태로 Shift+Home+Delete 하면 아무것도 안 지워져 URL 원문이 발행물에 남는다. Backspace로 URL 줄 끝에 복귀 후 선택·삭제해야 함. (subagent 구현을 diff 리뷰로 잡아낸 케이스 — 발행 경로는 키 시퀀스를 커서 위치 기준으로 시뮬레이션하며 리뷰할 것.)

4. **스펙의 매칭 키워드는 실제 데이터 enum과 대조**: `_expand_tags` 스펙이 영문 키워드(restaurant/cafe)만 나열했는데 실데이터 `vision.scene_type`은 한글 enum(맛집/신사/사찰)이라 그대로면 무동작이었다. fast-worker가 발견·보정. 매칭 로직 스펙을 쓸 때 실제 필드 값 도메인을 함께 명시할 것.

5. **PIL Image.open은 with로**: Windows에서 핸들이 안 닫혀 임시파일 os.remove가 PermissionError. `with Image.open(p) as src:` 필수.

## 2026-07-04 — 문체 따라하기 실효성 버그 2건 (사용자 "넣었는데?" 추적)

1. **네이버 블로그 글 URL은 iframe 래퍼 — 그대로 긁으면 본문이 빈다**: `blog.naver.com/<id>/<글번호>`를 urllib으로 읽으면 래퍼 HTML만 와서 StyleAnalyzer가 정규식 폴백(tone=일반, 예시 0)으로 조용히 저하됐다. 실제 본문은 `PostView.naver?blogId=&logNo=`. → `_normalize_naver_url`로 정규화. **"기능이 돌았다"와 "기능이 효과를 냈다"는 다르다 — 폴백 경로로 빠졌는지 extracted_by를 확인할 것.**

2. **state에만 있는 분석 결과는 재시작에 소실**: `/api/style` 결과가 자동저장 훅(분석완료·plan저장·발행성공) 밖이라 서버 재시작(에이전트가 자주 함!)에 날아갔다. 사용자가 "넣었는데" 어제 생성이 문체 없이 돈 원인. → 성공 시 `_save_project()` 즉시 호출 + 빈 세션 가드 완화(`_SESSION_KEYS` 중 하나라도 있으면 저장). **state 슬롯을 새로 채우는 API를 만들면 "이 값은 어느 훅에서 영속되나"를 반드시 확인.**

3. **진단 순서 교훈**: "안 되는데?" → 설정에 입력값 존재 확인(settings/profiles) → state/영속 파일에 결과 존재 확인(project.json) → 폴백 여부(extracted_by/warning) 확인. 이번엔 셋 다에서 각각 다른 문제가 나왔다(입력 O, 영속 X, 폴백 O).

## 2026-07-05 — 업로드 실패(doctype 에러) 근본수정 + 어제 오진 정정

1. **어제(7/4) 오진 정정 — "시스템 파이썬" 아니었다**: 시스템 Python310엔 flask 자체가 없어 서버로 못 뜬다. venv 런처(Scripts/python.exe)는 자식 프로세스 CommandLine이 기본 인터프리터 경로로 보이므로 **CommandLine/Path로는 venv 여부를 구분할 수 없다**. 진짜 원인은 아래 2~4의 복합.
2. **프론트 fetch는 r.ok를 먼저 확인하라**: 서버가 HTML(413/500 디버그 페이지)을 주면 r.json()이 "Unexpected token <"로 죽어 원인이 가려진다. debug=True Flask는 모든 오류가 HTML로 나간다.
3. **대용량 업로드는 배치 분할이 기본**: 전체 사진 한 요청 = MAX_CONTENT_LENGTH(500MB) 초과 시 라우트 밖(Werkzeug)에서 HTML 413 → 서버 로그에도 안 남아 오리무중. 25장/120MB 배치 + append=1 + 413 JSON errorhandler로 해결.
4. **업로드도 영속화 대상**: state["photo_paths"]가 메모리 전용이라 서버 재시작에 업로드가 증발했다(7/4 71장 유실 — 에이전트 재시작이 사용자 업로드를 지운 사고). state 슬롯을 채우는 모든 라우트는 _save_project() 훅 점검.
5. **포트 중복은 코드로 원천 차단**: netstat 확인 규칙(lessons #7d)을 사람이 지키게 하지 말고, app.py 기동 시 5000 점유 감지 → 안내 후 exit 1. 재발 사고 클래스를 가드로 봉인(실검증: 2중 기동 차단 확인).
6. **에이전트 백그라운드 서버는 로그를 파일로 남겨라**: -WindowStyle Hidden으로 띄우면 stdout이 증발해 사후 진단 불가. -RedirectStandardOutput/Error로 server.out.log/server.err.log 캡처(gitignore *.log 커버).

## 2026-07-05(2) — "2·3일차 못 불러옴" = 생성 단계 3중 결함

1. **config.json이 DEFAULTS를 조용히 덮는다**: openai.max_tokens가 config.json에 3000으로 박혀 있어(기본 16000) 사진 많은 일차(34/62장)만 응답이 잘렸다. 코드 기본값을 봤다고 끝이 아니다 — **gitignore된 로컬 config의 실제 값**을 확인할 것. 증상이 "특정 그룹만 실패"면 그룹 간 차이(사진 수→출력 길이)를 먼저 의심.
2. **실패를 빈 결과로 승격시키지 마라**: _call_ai가 None 반환 → 상위가 "0개 초안 생성 완료✅" → 전체 생성은 "전체 4개 그룹 완료✅" 허위 보고. 사용자는 편집 단계 blocks 400에서야 "못 불러온다"로 만난다. **빈 결과 = error 상태로 보고**하고, 전체 작업은 실패 항목을 이름으로 명시("2일차 생성 실패 — 해당 일차만 다시 생성").
3. **finish_reason을 읽어라**: "Unterminated string (char 38)" JSON 에러의 진짜 신호는 choices[0].finish_reason=="length"였다. 잘림이면 파싱 시도 전에 감지 가능. 잘린 응답의 머리/꼬리 디버그 로그가 진단을 결정지었다(응답 6,170자에서 length → 3000토큰 상한 역산).
4. **대형 그룹은 AI 출력 요구량을 구조로 줄여라**: 사진별 [PHOTO:긴파일명]+2~3줄 설명 강제는 62장에서 출력 폭발. 20장 초과 시 장소당 대표 태그 1개(나머지는 코드 2단계 자동 배치) — 단 4o-mini는 이 규칙을 무시하고 전 사진을 태깅하기도 하므로(실측) max_tokens 확보가 주 방어선, 프롬프트 모드는 보조.
5. **관찰(후속 과제)**: 프롬프트 규칙 2 "말투: 친근한 구어체(~했어요)"가 하드코딩이라 문체 따라하기(평서체 ~였다)와 충돌 — style_ctx가 있으면 규칙 2를 양보시켜야 문체 반영이 완전해진다.

## 2026-07-05(3) — "순서 엉망·품질 허접" = 경로카드 오배치 + 도로명 캡션 + 문체 3중 상쇄

1. **정규식 선행 그룹이 앵커를 오염시킨다**: 경로카드 h2 매칭에 붙은 선행 그룹 때문에 m.start()가 이전 섹션의 아무 p 태그를 가리켜 카드가 출발지 사진 한복판/글 맨 앞에 꽂혔다. 삽입 위치 계산은 목표 요소만 매칭하고, 앞뒤 문맥은 별도 윈도우 탐색으로. 또 첫 매치 채택은 유사 이름(고베 X 5개)에서 오매칭 — 항상 최고 일치 선택.
2. **캡션의 도로/노선명은 품질 킬러**: Nominatim 세부명이 도로명일 때 부기가 그대로 노출됐다. 표시 계층에서 패턴 필터.
3. **문체 지시는 층층이 상쇄된다**: [참고 문체]를 넣어도 ①하드코딩 말투 규칙 ②설정 기본값(친근 구어체) ③모델의 해요체 prior가 차례로 되덮었다. 프롬프트에 같은 속성을 지시하는 조각이 몇 곳에서 생성되는지 전수 확인할 것.
4. **어미는 목록이 아니라 레지스터로 지시**: 어미 패턴 목록 주입 → 혼합체(였다+답니다 뒤섞임)로 악화. 지배 레지스터(평서체/해요체) 판별 후 system 메시지에 이진 지시("전부 -다로, 존댓말 금지")가 정답 — 평서체 43:2 달성. 4o-mini도 이진 지시는 잘 따른다(사용자 지적이 옳았다 — 모델 탓 전에 프롬프트 구조).
5. **품질 불만은 실물 골격 덤프로 진단**: h2/사진/이동카드 순서를 이벤트 리스트로 뽑으니(base64 치환) 3개 결함이 한눈에 나왔다. "AI가 순서를 어겼겠지" 추측은 셋 다 틀렸다(섹션 순서는 정확했음).

## 2026-07-06 — "사진 순서 뒤죽박죽" 근본수정 (2단계 자동배치 + AI 태그 불신)

1. **크기 창(char window) 휴리스틱은 base64 시대에 전부 지뢰**: 2단계 자동배치의 figure 체인 추적이 `fig_end + nf < 1000` 가정(사진이 짧은 URL이던 시절)이라 수백 KB base64 figure 앞에서 즉시 멈춤 → 전 유닛이 h2 바로 뒤(헤더 div 내부!) 같은 위치에 꽂혀 시간 역순 + 소개글 앞 배치. **위치 탐색은 "다음 구조 요소(h2/헤더 div)" 기준으로, 문자 수 창 기준 금지.** → `_section_end_pos`(순수함수)로 섹션 끝 삽입.

2. **AI 태그의 "위치"도 검증 대상이다**: 재생성마다 AI가 [PHOTO:] 태그를 듬성듬성(per-place 규칙 무시)·EXIF 역순으로 달아 순서가 다시 깨졌다(같은 코드, 생성마다 다른 증상). 사진 순서의 진실원은 계획검수/EXIF — 태그 위치가 유닛 순서와 함께 증가하는 **LIS만 앵커로 채택**, 강등분은 다음 앵커 앞에 병합(시간순 보존), 꼬리만 섹션 끝. 그리디 단조 검사는 역전 태그가 먼저 승격돼 정상 태그를 밀어냄 — LIS가 정답.

3. **텍스트 마커 삽입은 태그 내부 위험**: '여행 꿀팁' 마커가 `<h3>🧳 여행 꿀팁` 태그 안이라 그 위치 삽입 시 h3가 깨짐(옛 코드 잠복 결함). 텍스트 마커를 찾으면 래퍼 여는 태그(`<div`)로 rfind 되짚어 그 앞에 삽입.

4. **E2E는 "재생성 1회 성공"으로 끝나지 않는다**: 1차 재생성은 완벽했는데 2차 재생성에서 AI 태그 변동으로 새 결함이 드러났다(2번 관찰). AI 개입 경로는 최소 2회 생성으로 변동성까지 확인. 검증은 골격 덤프 + **figure alt 시퀀스 vs EXIF 기대 시퀀스 자동 대조**(33/33)가 결정적이었다.

## 2026-07-06(2) — "발행하면 장소 순서 전혀 안됨" = 발행 라운드트립(html_to_blocks) 버그

앞선 draft 순서 수정(2026-07-06)은 맞았으나, 사용자가 **발행/로컬저장한 결과물**은 여전히 뒤죽박죽이었다. 진범은 draft가 아니라 **발행 경로**.

1. **에디터 미리보기(원본 content)와 발행물(블록 라운드트립)은 다른 파이프라인**: 발행·로컬저장은 `post["content"]` → `html_to_blocks()`(posters) → blocks → `blocks_to_html()`(app) 을 거친다. draft content는 순서가 맞아도 이 변환에서 깨질 수 있다. **"에디터에서 멀쩡한데 발행하면 이상"이면 draft가 아니라 발행 변환을 의심.**

2. **콜라주 figure의 alt 재매칭이 원흉**: draft는 2장을 1개 콜라주 figure(alt=장소명)로 합친다. `html_to_blocks._match_photo`는 figure당 alt로 **1장만** 매칭 → 콜라주 짝 ~40%가 미매칭 잔여로 남았다.

3. **잔여 사진 덤프가 인트로 코스라인으로 쏟아짐**: 잔여 사진을 "장소명이 `content`에 포함된 첫 텍스트 블록" 뒤에 넣는데, **인트로 코스라인("A → B → C …")이 모든 장소명을 담고 있어** 전 장소의 잔여 사진이 글 맨 위(첫 H2 이전)로 무더기로 몰렸다. (고베 34장 중 14장이 인트로 뒤에 적체.)

4. **수정 = 순서의 진실원을 figure에 각인**: `_photo_html(src_files=...)`로 figure에 `data-files="fn1|fn2"`(구성 원본 파일명, 순서 유지)를 심고, `html_to_blocks`가 그 목록을 **제자리에서 순서대로 개별 image 블록으로 전개**(alt 재매칭·잔여 덤프 우회). 발행 순서 == 계획/EXIF 순서를 코드가 보장.

5. **검증은 발행 블록을 계획 파일명 순서와 대조**: `html_to_blocks(content, photo_results)`의 image 블록 basename 시퀀스 == `photo_results` 파일명 순서, 그리고 "첫 장소 H2 이전 이미지 수 == 0"을 4개 그룹 전수 확인(6/34/62/7 전부 일치). draft 골격만 봤으면 못 잡았다 — **사용자가 실제로 보는 산출물(발행물)의 경로로 검증할 것.**

## 2026-07-06(3) — 실제 네이버 발행 E2E 검증 완료 (순서 수정 2건 확증)

발행 순서 수정(`804e755` 생성경로 + `5694083` 발행경로) 후, **실제 네이버 발행**으로 끝까지 확증했다.

1. **검증된 사실**: 고베(G1, 8장소·사진 34장) 비공개 실발행 성공 → `blog.naver.com/turetle008/224338033108`. 발행 파이프라인(`html_to_blocks`→`blocks_to_html`) 산출물을 브라우저 렌더로 육안 확인(지역정보→코스요약→장소별 소개→사진→경로카드, **상단 사진 적체 0**, 11:45 버그의 14장 적체 재현 안 됨). chromedriver 잔존 0(정상 close, 세션 보존), 로그인 세션 살아있어 수동로그인 생략.

2. **발행 진행표시 함정**: `/api/publish` task가 상태 메시지를 "발행 준비 중"으로 1회만 세팅하고 이미지 삽입 루프 동안 갱신 안 함 → 5분 넘게 "준비 중"으로 보여 멈춘 걸로 오인. 실제로는 `server.out.log`의 `naver_img{N}` 로그로 진행 확인됨(34장 클립보드 발행 ≈ 12분). **"멈춘 듯 보임"은 상태 메시지가 아니라 서버 로그(이미지 카운터)로 판정할 것.** (후속: 발행 task에 per-image progress_cb 배선하면 UX 개선.)

3. **전 기능 자동 대조 스크립트가 조각검증 왕복을 끝냈다**: 발행 블록 image 파일명 시퀀스 == `photo_results` 순서 + "첫 장소 H2 이전 이미지 0" + 11개 기능을 4일차 전수 매트릭스로 확인. draft 골격만 보던 이전 검증이 발행경로 버그를 놓쳤던 교훈(2026-07-06(2))의 재발 방지책 — **사용자가 실제 보는 산출물(발행물) 경로로 검증**.

## 2026-08-17 — 발행 진행표시 + 계획검수 과분할 근본수정 (측정이 우선순위를 뒤집은 세션)

1. **"다음 할 일"을 코드 독해로 정하지 말고 실데이터로 검증하라**: 핸드오프의 최우선 잔여였던 "place_groups가 비연속 재방문을 병합"은 코드만 보고 적은 관찰이었다. 실제 plan.json을 재니 **stop 시간구간 교차 0건** — 그 층(GPS location_name)은 섹션 구조를 결정하지도 않았다(섹션은 **plan stop** 기준. `groups_from_plan`이 stop 이름으로 location_name을 덮어씀). 그대로 "run 단위 그룹핑"을 구현했으면 3일차 섹션이 6개→49개로 폭발할 뻔했다. **측정 없이 착수했으면 개선이 아니라 사고.**

2. **진짜 결함은 옆 칸에 있었다 — 계획검수 자동 stop 과분할**: 자동 초안 31개 stop → 사용자가 손으로 6개로 정리(2일차 20→8, 1일차 5→3). 원인은 `_stops_for_day`의 '확신 이름 키 + ~110m 격자': 무료 모드 역지오코딩 세부명이 같은 장소 안에서 관광지/공원/거리/신사/주차장으로 갈리고 격자 경계까지 겹친다. → **시간·거리 세션 클러스터링**(45분·300m)으로 교체. 이름 품질에 의존하지 않는 게 무료 모드에서 결정적.

3. **사용자의 확정본은 공짜 정답 레이블이다**: plan.json의 확정 stop을 ground truth로 두고 사진쌍 P/R/F1로 임계값을 그리드서치했다(이름키+격자 0.456 → 45분/300m **0.940**, 6개 일차). 과병합=사용자 '분리' 노동, 과분할='합치기' 노동으로 지표를 노동량에 대응시키니 파라미터 선택이 논쟁이 아니라 계산이 됐다. 직관으로 고른 v2(확신 이름 경계 추가)는 오히려 0.841 — **그럴듯한 개선안이 실측에서 지는 것도 흔하다.**

4. **한 층을 고치면 다음 층의 계약이 드러난다(연쇄 3단)**: ① 클러스터링이 재방문을 분리 → ② 하류가 전부 `name` 키로 묶으므로(place_groups·프롬프트 장소목록·memos/meta) 동명 stop이 한 섹션으로 재병합되고 메모·별점이 서로를 덮어씀 → "(재방문)" 접미로 구분 → ③ 재초안 시 사용자가 합쳐둔 장소가 다시 쪼개져 **실제 재방문이 아닌데 "(재방문)" 섹션**이 생김 → 연속 동명은 재방문 아님 + `merge_user_edits`가 사용자 확정 묶음을 되붙임. ③은 실데이터 재초안을 돌려보고서야 나왔다 — **새 기능은 "기존 사용자 데이터로 한 번 더 돌려보는" 경로까지 검증할 것.**

5. **되돌리기 어려운 작업의 진행표시는 기능이다**: 발행 task가 상태를 1회만 세팅해 사진 34장 삽입(≈12분) 동안 "발행 준비 중"으로 멈춘 듯 보였다. `progress_cb`를 블록 단위로 배선(준비/에디터/제목/블록별+사진 카운터/발행창/공개설정/태그/최종확인). 퍼센트 계산은 순수함수(`publish_progress`)로 빼서 단독 테스트, 배선은 **Flask 라우트 + /api/status 폴링 E2E**로 확인(스냅샷 14개·단조증가). 발행기 스텁으로 네이버 없이 검증 가능.

6. **E2E 스크립트는 사용자 산출물을 건드리지 않게 설계하라**: `/api/plan/draft`는 plan을 덮고 `drafts/` 폴더를 지운다. 검증 시 `_plan_dir`/`_save_plan`/`_save_project`를 임시 디렉터리·no-op로 갈아끼우고 "디스크 기록 없음"까지 assert했다. 실생성 E2E도 저장 경로를 안 타는 `generate_drafts` 직접 호출로.

7. **함정 — `time` 모듈은 app과 공유객체**: E2E에서 `A.time.sleep = lambda ...`로 스팸방지 대기를 스킵하려 했더니 내 지연 흉내까지 사라져 전 구간이 순식간에 끝나며 폴링이 아무것도 못 잡았다("진행표시 실패"로 오독할 뻔). 모듈 속성 패치는 프로세스 전역이다.

## 2026-08-17(2) — 무료 위치 공유 설계 + 입력 표면 정리

1. **"무료로 되게 해줘"는 먼저 무료 후보의 실제 산출물을 긁어보고 답한다**: 지도 OG 카드를 키 없이 만들려고 후보 URL들의 og 태그를 직접 크롤한 결과 — 구글맵(키 없음)은 og:title이 비어 "Google 지도" 일반 카드, 네이버 지도 URL은 OG 자체가 빔(SPA), OSM은 "OpenStreetMap" 일반 카드. **무료로는 리치 카드가 불가능**이라는 게 추측이 아니라 실측으로 확정됐고, 설계가 "카드 흉내" 대신 "하이퍼링크로 좌표 공유"로 바뀌었다.

2. **OSM 타일 스크린샷은 정책 위반 소지**: 타일 사용정책이 "헤드리스 봇이 팬/줌으로 강제 렌더링"을 금지 항목으로 명시한다(=Selenium 지도 캡처가 정확히 그 패턴). 구글맵 캡처도 Maps Platform 약관상 재배포 제한. 무료 지도 이미지는 라이선스부터 확인할 것.

3. **목적을 물어야 설계가 산다**: 사용자가 "100% 자동화가 목표가 아니다. 장소는 스토리를 위해 묶고, **방문객에게 정확한 위치를 공유**하려는 것"이라고 정정해 줬다. 그 한 문장으로 정답이 '위키 정보 카드'에서 '좌표 하이퍼링크 + 검수에서 사용자가 링크 교체'로 바뀌었다. 기능의 목적을 모르면 실측을 해도 엉뚱한 걸 만든다.

4. **신뢰 기반 판정은 발동률이 낮을 수 있고, 그게 정상이다**: 위키 카드는 이름 정확일치+좌표 30km 교차검증을 걸었더니 사용자 확정명 20개에서 **채택 0건**(랜드마크 정확명은 5/6 채택). 조용히 생략되므로 해롭지 않지만, "구현했다"와 "쓸모 있다"는 다르다 — 발동률을 실데이터로 재고 보고할 것.

5. **죽은 입력은 위험하다**: 설정 화면의 네이버 ID/PW는 발행기가 `os.environ`만 읽어 **한 번도 쓰이지 않으면서 settings.json에 평문 저장**되고 있었다. 입력을 지울 때는 (a) 저장 파일에서도 제거 (b) 마이그레이션 멱등성 (c) 저장 함수가 다시 쓰지 않는지 테스트까지 묶어야 한다.

6. **UI를 고쳤으면 반드시 실제로 띄워 본다(재확인)**: `advOpen`을 선언 없이 참조하는 코드를 커밋 직전에 잡았다(lessons 2026-07-01 #9a와 같은 사망 패턴). 또 `/browse`로 보고서야 참고 블로그 URL 입력이 **4단계가 아니라 3단계**에 있다는 걸 알고 안내 문구를 고쳤다 — 코드만 읽고 쓴 안내는 틀리기 쉽다.

7. **자동화 검증에서 base64는 오탐원**: "본문에 URL 없음" 검사에 `"http" not in html`을 쓰면 base64 이미지 안의 우연한 `http` 바이트열에 걸린다. URL 검사는 `https?://` 스킴 패턴으로, 가능하면 data URI를 먼저 제거하고.

8. **외부 LLM 자문은 실행 환경부터 확인**: Gemini는 MCP(실행파일 누락)·CLI(API 키 무효) 둘 다 실패했고 Codex CLI만 응답했다(둘 다 "신뢰되지 않은 디렉터리" 가드에 먼저 막혀 `--skip-git-repo-check`/`GEMINI_CLI_TRUST_WORKSPACE=true` 필요). **자문 결과가 없으면 없다고 보고**하고 단독 판단임을 밝힐 것.

## 2026-08-17(3) — UI/UX·플로우 정리 (6단계 → 4단계)

1. **"느린 화면"의 원인은 프론트가 아니라 API였다**: 계획검수가 6초간 "불러오는 중"에 묶인 진범은 `/api/photos`가 호출마다 사진 109장의 썸네일을 디스크에서 다시 인코딩한 것(7.97초·1MB). (경로, mtime, 크기) 키 캐시로 **0.26초(31배)**. 프론트는 계획을 먼저 그리고 썸네일이 오면 다시 그리게 분리. **화면이 느리면 렌더 코드를 뜯기 전에 각 API의 응답시간부터 재라**(`curl -w %{time_total}`).

2. **단계 번호를 코드에 흩뿌리면 재배치가 지뢰밭**: `go(3)`, `step===5`처럼 숫자가 12곳에 박혀 있어 6→4단계 재구성 시 한 곳만 놓쳐도 조용히 엉뚱한 화면이 뜬다. `PREP/PLAN/DRAFT/PUB_STEP` 상수로 바꾸고 전수 치환. 내부 함수명도 `renderS1`(=STEP2였다)처럼 어긋나 있어 역할 이름으로 정리.

3. **"초안 있음"의 진실원이 둘이다**: `has_drafts`(메모리)와 `has_saved_drafts`(디스크). 재시작 후엔 후자만 참이라 전자만 보면 "초안이 있는데 생성 화면"이 뜬다. 서버 재시작이 잦은 앱은 **상태 플래그마다 "재시작 후에도 참인가"를 확인**할 것.

4. **빈 패널은 공간 낭비가 아니라 UX 손실**: 오른쪽 미리보기가 준비·검수 단계에서 "사진을 업로드하면…" 안내만 띄우며 화면 절반을 먹고 있었다. 결과물이 있는 단계(원고·발행)에서만 띄우고 나머지는 본문 전폭(`.app.no-preview`)으로 — 검수 화면에서 장소 카드·스토리 입력이 훨씬 편해졌다.

5. **삭제한 설정의 잔재는 다른 화면에 남는다**: 설정에서 지운 "발행 방식"이 발행 화면에 그대로 살아 있었다. 설정 항목을 제거할 때는 **그 값을 읽는 모든 화면**(`grep`으로 id·키 이름 전수)까지 훑을 것.

6. **사장 코드는 실제로 죽었는지 DOM 기준으로 확인**: `renderPE/loadGrps/chMode/renamePG/updPhoto`가 참조하는 `#peList/#grpList/#modeSel`이 파일에 0개 — 그래서 안전하게 30행 제거. 함수 참조 수만 보면 "쓰이는 코드"로 착각한다(`nextFromPlan`이 `loadGrps`를 호출하고 있었지만 그 함수가 아무 DOM도 못 찾아 무동작이었다).

7. **UI 리팩터는 단계마다 파서 + 실렌더 이중 검증**: 인라인 스크립트라 구문오류 1개면 SPA 전체가 죽는다. 매 편집 후 `node vm.Script` 파싱 → 전 단계 `/browse` 렌더 + 콘솔 에러 0 확인 → 스크린샷 육안 확인. 이번에도 스크린샷을 보고서야 "다음 버튼 없음", "옛 단계 번호 안내문"을 찾았다.
